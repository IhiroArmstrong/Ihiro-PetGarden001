# Cursor Prompt：实现本地导出/导入功能

> 使用方式：把下面「Prompt 正文」整段粘贴给 Cursor（建议用 Agent 模式，允许它读写多个文件）。
> 「实现前必读」里列的文件路径需要你在粘贴前替换成仓库里的真实路径。

---

## 实现前必读（粘贴前替换占位符）

- `{{PRACTICE_BACKUP_SNAPSHOT_FILE}}` → 现有 `practiceBackupSnapshot.js`（或 .ts）的实际路径
- `{{SETTINGS_SCREEN_FILE}}` → 设置页 / 数据与隐私页面的实际路径
- `{{I18N_DIR}}` → 多语言文案目录
- `{{TEST_DIR}}` → 单测目录约定

---

## Prompt 正文（直接粘贴给 Cursor）

我要在现有 App 中实现「本地导出/导入」功能。这是一个纯本地操作，**不涉及任何网络请求、不涉及云端**。请先阅读下面的背景和需求，再开始改代码，遇到不确定的地方先问我，不要自行猜测覆盖策略或数据范围。

### 背景

仓库里已经有云端备份用的快照序列化逻辑，位于 `{{PRACTICE_BACKUP_SNAPSHOT_FILE}}`：

- `serializePracticeBackupSnapshot(storage, now)` — 从 localStorage 读取白名单 key，打包成 `{ schemaVersion, savedAt, stores }` 的 JSON 快照
- `PRACTICE_BACKUP_STORE_KEYS` — 当前白名单包含 6 个 key（Journey、练习日、里程碑、ownership、仪式、芥子印，具体以代码为准）
- `PRACTICE_BACKUP_SCHEMA_VERSION` — 当前 schema 版本号
- 应该还有对应的解析/写回函数（如 `parsePracticeBackupSnapshotClient` / `writePracticeBackupStoresRaw`），请在仓库里确认实际函数名

**本地导出/导入必须复用这套快照格式**，不要另起一套 schema。导出的文件结构应与云端备份快照的 JSON 结构保持一致，这样两套机制未来可以互相兼容（比如「导出的文件」理论上也能被「云端备份恢复」的解析逻辑读取）。

### 功能范围

只做「本地导出」和「本地导入」两个功能，**不要碰云端备份相关的 UI 或后端逻辑**（云端备份入口目前是隐藏状态，保持隐藏，不要重新显示或删除相关代码）。

---

### 一、导出功能

**入口位置**：设置 › 数据与隐私 页面（`{{SETTINGS_SCREEN_FILE}}`），新增一个「导出数据」的操作项。

**交互流程**：

1. 用户点击「导出数据」
2. 调用 `serializePracticeBackupSnapshot` 生成快照 JSON
3. 生成文件名：`{app名}-backup-{YYYY-MM-DD}-{HHmmss}.json`（带时间到秒，避免同一天多次导出时文件名冲突）
4. 调起系统的文件保存/分享面板（Web 用 File System Access API 或退化为 `<a download>`；如果是移动端壳，用对应平台的分享 API），让用户自己选择保存位置
5. 分享/保存动作完成后，展示一个轻量的成功提示（toast，非阻塞弹窗），文案："已导出到你选择的位置"
6. 若生成快照或写文件失败，展示明确的错误提示，不要静默失败

**不需要**：不需要在导出前弹确认框，导出是无损、非破坏性操作。

---

### 二、导入功能

**入口位置**：同一设置页，「导出数据」旁边加「导入数据」。

**交互流程（严格按此顺序，每一步失败都要终止后续步骤，不能部分执行）**：

**Step 1 — 选择文件**
用户点击「导入数据」→ 调起系统文件选择器，限制文件类型为 `.json`。

**Step 2 — 校验文件**
读取文件内容，做以下校验，任一失败则进入错误态，不写入任何数据：
- JSON 能否正常 parse
- 是否包含 `schemaVersion` / `savedAt` / `stores` 字段
- `schemaVersion` 处理规则：
  - 等于当前版本 → 直接进入 Step 3
  - 低于当前版本 → 尝试用已有的迁移逻辑升级（如果仓库里没有迁移机制，就在这里预留一个 `migrateSnapshot(snapshot, fromVersion, toVersion)` 的空实现并打 TODO，不要瞎写迁移逻辑）
  - 高于当前版本 → 直接拒绝，报错文案："此备份文件版本高于当前应用支持的版本，请更新应用后重试"
- `stores` 里的 key 是否都在当前 `PRACTICE_BACKUP_STORE_KEYS` 白名单内（多余 key 忽略即可，不报错；但要在日志里记一条 warning）

校验失败的错误态：展示错误提示 + 一个"重新选择文件"的按钮，不展示任何本机数据。错误文案要说明具体原因（文件损坏/格式不对/版本过旧过新），不要只说"导入失败"。

**Step 3 — 预览摘要**
校验通过后，展示一个摘要卡片（不是弹窗，是页面内容），包含：
- 该文件的 `savedAt`（保存时间）
- 每个数据类别的条目数量（如"习惯打卡记录 156 条"、"练习日历 51 天"），需要遍历 `stores` 里每个 key 对应数组/对象算出条目数，展示逻辑要能适配不同 key 的数据结构（有的是数组、有的可能是单个对象，需要写一个通用的"计数"函数，无法计数的类别显示"已包含"即可，不要报错）

**Step 4 — 覆盖确认**
这一步是核心，判断本机是否已有数据：

- **若本机对应的白名单 key 全部为空**（新设备/新安装场景）：跳过覆盖警告，直接展示一个轻量确认（比如"导入以下数据？"+ 确认/取消），不需要危险色按钮和勾选框。
- **若本机已有数据**：展示覆盖确认弹窗，弹窗内容：
  - 标题：「导入将覆盖本机数据」+ 警告图标
  - 一行说明：此操作会用文件内容整体替换对应类别的本机记录，不可撤销
  - 一个对比表格：每个数据类别显示「本机 vs 导入文件」的条目数量，导入文件数值用强调色（accent）标出
  - 若某个类别「导入文件数量 < 本机数量」，额外展示一条警示条（warning 底色 + info 图标），提示"导入文件的这项数据少于本机，继续操作会丢失本机独有的记录"
  - 最下面显示导入文件的"最后保存于"时间
  - 一个必须勾选才能继续的复选框："我已了解此操作会覆盖本机数据"
  - 两个按钮：「取消」（默认态）+「确认覆盖并导入」（危险色 `--fill-danger`，勾选前禁用，勾选后才可点击）

**Step 5 — 原子写入**
用户确认后：
1. 先把要写入的数据在内存里准备好（不要边解析边写 localStorage）
2. 全部准备好之后再一次性写入所有白名单 key（尽量做到要么全部写入成功、要么全部不写，如果框架层面做不到真正的事务写入，至少要保证：**先备份当前本机数据到一个临时变量，写入失败时能回滚**）
3. 写入成功后展示成功提示："已导入，数据已更新"，并触发页面/状态刷新（如果 App 里有全局状态管理，需要 dispatch 相应的刷新 action，不要指望用户手动下拉刷新才能看到新数据）
4. 写入失败：展示错误提示，尝试回滚到写入前的本机数据，并在日志里记录详细错误

---

### 三、数据范围与不做的事

- 导出/导入范围严格等于 `PRACTICE_BACKUP_STORE_KEYS` 白名单，**不要**自作主张扩展到 reflections、Confide 对话记录等敏感/未在白名单里的数据。如果代码里发现相关数据结构，忽略即可，不要加进去。
- **不做**端到端加密。导出文件是明文 JSON，这是当前范围内已知且接受的行为，不需要额外加密逻辑。
- **不碰**云端备份相关代码（如已存在的 `practiceBackupKv` 相关服务端逻辑），除非我明确要求。

---

### 四、文案与 i18n

所有面向用户的文案需要走现有的 i18n 机制（参考 `{{I18N_DIR}}` 下的现有 key 命名风格），至少包含中文（简体）文案，其他语言可以先留 TODO 或用中文兜底，不要因为缺翻译阻塞功能开发。

文案基调参考：
- 按钮用动词开头、简短，不用感叹号
- 错误提示说清楚"发生了什么 + 现在能做什么"，不用"Error:"前缀
- 确认类文案不要用"成功"这种自吹自擂的词，直接说结果，比如"已导出"而不是"导出成功！"

---

### 五、测试

在 `{{TEST_DIR}}` 下补充单测，至少覆盖：

- 导出：空库导出（各 key 为空/undefined 时不报错）、正常数据导出、生成的文件名格式正确
- 导入校验：合法文件、JSON 格式错误、缺字段、schemaVersion 过高、schemaVersion 过低（有迁移逻辑时验证迁移结果）
- 导入覆盖判断：本机为空时跳过覆盖警告、本机非空时展示覆盖警告、导入文件某类别数量少于本机时展示警示条
- 原子写入：模拟写入中途失败时能回滚到写入前状态
- 计数函数：能正确处理数组、对象、undefined 等不同数据结构而不抛错

### 六、验收标准（Definition of Done）

- [ ] 设置页新增「导出数据」「导入数据」两个入口
- [ ] 导出的文件可以被导入功能重新读取并成功还原数据（自我闭环验证）
- [ ] 覆盖确认弹窗按上述设计展示本机 vs 导入文件的数据对比
- [ ] 勾选框未勾选时确认按钮不可点击
- [ ] 所有失败路径（文件损坏、版本不兼容、写入失败）都有明确的用户提示，且不会导致本机数据处于损坏或部分覆盖的中间状态
- [ ] 未改动任何云端备份相关的现有代码
- [ ] 新增单测通过

如果你在实现过程中发现现有 `practiceBackupSnapshot` 相关函数的实际签名、导出方式与我上面描述的不完全一致，请以仓库里的实际代码为准，并告诉我做了哪些调整。
