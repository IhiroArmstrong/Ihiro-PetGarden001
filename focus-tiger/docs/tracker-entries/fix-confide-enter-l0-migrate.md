| Confide Enter 发送 + 1.7B 型号行 + 0.6B 残留清理（#419） | UI可见 | 待人工测试 | **Electron 宽屏 · QA 树 `85a25bd`**：`npm run desktop:dev` 或 Vite+手动 Electron。① 输入框 Enter 发送（非换行）。② 状态条显示 **1.7B** 型号。③ **0.6B 清理（可选文件核对）**：`~/Library/Application Support/Focus Tiger/companion-l0/` 在 1.7B 完整后应**无** `Qwen_Qwen3-0.6B-Q4_K_M.gguf`（及 `.meta.json` / `.part`）；仅有 `Qwen3-1.7B-Q4_K_M.gguf`。若本机从未下过 0.6B，以产品表现（1.7B 标识 + 问答正常）为准。 | **2026-08-25 用户书面（QA 树 `85a25bd` · Electron）**：Enter 发送 OK；状态条 1.7B OK；问答表现挺好；**不知如何在产品内看出 0.6B 是否已清理**（未做 Finder 核对）。 | — | — | PR #419 · `l0SpikeSeed.retireLegacyProductionGgufs` | 2026-08-25 |
