| Voice Input · Speak to type audio engine abort on Stop | UI可见 | 有问题 | **不要用 5173 / 合 #978 前的打包 App。** `cd focus-tiger && npm run desktop:dev`（宽屏 Electron）→ Confide → Speak to type → 允许语音识别（若弹出）→ 说 `I feel tired` → Stop → 字进框；再说 `What are you doing.`。红字不得再是 `Speech input failed (recognition_failed).`；若仍失败，红字应带引擎/helper 细节而非空码。回流：关倾诉再开再说一句。单测：`macosSpeechNative.test.js` · `speechProvider.test.js` | **2026-09-26**：#978 合入后 desktop:dev 两次仍 `Speech input failed (recognition_failed).`；本机 crash 报告为 `AVAudioEngine.prepare()` abort。**2026-09-26 复测 #979（QA 树 f801396）**：目录与 `sync:qa-develop && desktop:dev` 正确；英文两句仍失败；Stop 后约五六秒红字 `No speech was heard. Try again or type instead.`（非崩溃文案）；微信语音转文字正常。 | release-blocker | 崩溃已换成空转写；诊断见 fix-voice-input-empty-speech-diagnostics | `macos-speech-helper.swift` · `macos-speech-exception-trap.m` · `macosSpeechNative.js` | 2026-09-26 |
