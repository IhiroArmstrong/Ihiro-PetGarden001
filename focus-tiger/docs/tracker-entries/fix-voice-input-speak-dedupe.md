| Voice Input · Speak again dedupe + clear field + layout | UI可见 | 待人工测试 | **不要用 5173。** 停掉旧的 `desktop:dev`，在本旁支 `cd focus-tiger && npm run desktop:dev`（宽屏 Electron）→ Confide → Speak to type → 说 `I feel tired` → Stop → **框内只出现一遍**（不重复）。再点 Speak again → 说 `What are you doing.` → **旧字已清空**、只留新句。🎙 按钮与输入框之间应有明显间距（不再贴住）。长句超 280 字 → 琥珀色提示截断。回流：关倾诉再开。单测：`voiceInputBridge.test.js` | **2026-09-26**：#983 后字能进框，但每次重复一遍；新录入不清旧字；Speak again 贴住文字框；长句像被截断。 | release-blocker | 去重（status+stop 双写）；Speak 前清框；margin 修正；截断提示 | `VoiceInputChrome.js` · `voiceInputBridge.js` | 2026-09-26 |
