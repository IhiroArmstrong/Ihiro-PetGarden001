| 冷启动目标卡后 Yin 消失（吹花日） | UI/交互 | 待人工测试 | Safari `?product=1`：清 `flower-welcome.v1` + `cold-start-goal-seen.v1` + session choice → 吹花 → 白玉气泡 → **四选卡弹出后、点选项前**阿寅须仍可见（闭目坐禅）；点 Study or work → 20 分钟呼吸时阿寅仍在。对照：只清目标卡 key、不吹花的 1 分钟路径此前已过。**5173 QA 树须 `sync:qa-develop` 后硬刷新复测。** | 2026-09-12：5174 激进版（四选 onOpen 补 idle）已过；合入 #723 精简版缺 onOpen 一刀 → 5173 四选后无阿寅。**2026-09-12 本 fix**：四选卡 `onOpen` 再调 `ensureIdleBaselineAfterWelcome`（叠化、无 restart）。**2026-09-12 5173 tip `40d5298` 复测（#726 已合）**：四选卡后阿寅**已可见**（毛玻璃后闭目坐禅）——本行主诉体验确认通过，仍待关单门禁。**同轮新发现**：吹花动画看不清 → 气泡 `onHidden`（≈4.1s）早于 `conjureFlowersBlowAway`（≈6.5s）完成就叠化回 idle，切掉吹散尾段；已另立 `ISSUE_LEDGER` 条目，**不在本行关单范围**。 | — | — | `main.js`（吹花 onComplete/气泡 onHidden + **四选 onOpen** `ensureIdleBaselineAfterWelcome` + 微仪式 `onBreathStart` 门闩） | 2026-09-12 |
