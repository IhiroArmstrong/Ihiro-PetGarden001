| 冷启动目标卡后 Yin 消失（吹花日） | UI/交互 | 待人工测试 | Safari `?product=1`：清 `flower-welcome.v1` + `cold-start-goal-seen.v1` + session choice → 吹花 → 白玉气泡 → **四选卡弹出后、点选项前**阿寅须仍可见（闭目坐禅）；点 Study or work → 20 分钟呼吸时阿寅仍在。对照：只清目标卡 key、不吹花的 1 分钟路径此前已过。**5173 QA 树须 `sync:qa-develop` 后硬刷新复测。** | 2026-09-12：5174 激进版（四选 onOpen 补 idle）已过；合入 #723 精简版缺 onOpen 一刀 → 5173 四选后无阿寅。**2026-09-12 本 fix**：四选卡 `onOpen` 再调 `ensureIdleBaselineAfterWelcome`（叠化、无 restart）。 | — | — | `main.js`（吹花 onComplete/气泡 onHidden + **四选 onOpen** `ensureIdleBaselineAfterWelcome` + 微仪式 `onBreathStart` 门闩） | 2026-09-12 |
