| Voice Input · on-device STT run loop (Speak to type empty despite audio) | UI可见 | 待人工测试 | **不要用 5173。** 停掉旧的 `desktop:dev`，在本旁支 `cd focus-tiger && npm run desktop:dev`（宽屏 Electron）→ Confide → Speak to type → 说 `I feel tired` → Stop → **字应进框**（不必等满 6 秒红字）。再说 `What are you doing.` 回流：关倾诉再开。若仍失败：红字仍带 `buffers=` / `peak=` 诊断；终端 `[voice-input] empty transcript`。**读数**：`peak` 明显大于 0 仍无字 = 识别仍空（本 PR 修的是主线程卡死导致回调不跑）。单测：`macosSpeechNative.test.js` · `speechProvider.test.js` · `voiceInputBridge.test.js` | **2026-09-26**：#981 诊断确认 peak≈0.03、buffers≈90、48000Hz，仍空转写；Stop 后约 6 秒才出红字（与 helper 6s 超时一致，暗示识别回调未跑）。 | release-blocker | 修 macos-speech-helper 主线程 pumpRunLoop + 先开 recognitionTask | `macos-speech-helper.swift` | 2026-09-26 |
