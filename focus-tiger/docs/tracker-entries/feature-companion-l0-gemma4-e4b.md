| Gemma4-E4B 生产 L0 接线（Mac 优先 · L3 换模） | 纯后端/桌面 | 待人工测试 | **仅 Electron 非低配宽屏。** `l0ModelProfiles.js` 默认 `gemma4-e4b`（jc-builds `Gemma-4-E4B-it-Q4_K_M.gguf` · ~5.0 GB）；`FT_COMPANION_L0_MODEL=qwen3-1.7b` 回退旧 Qwen。L3 `buildCompanionL2Prompt` 按 `promptFamily` 去 `/no_think`；规则桶 / crisis / corpus 路径未动。**人工**：宽屏 Confide → 首次下载 ~5GB → ready → 状态条 `Model4E4` → 非桶闲聊 `data-source=generate`；日/英/中各 2–3 句肉测。**Sanity**：系统终端 Metal TTFT/decode 一轮确认无异常掉档（非硬门禁）。**回退验**：设 `FT_COMPANION_L0_MODEL=qwen3-1.7b` 重启 → `Model317` + 旧 GGUF。 | PO 2026-09-16：1.7B 多次肉眼实测不足 ship，推翻 Gate 0.D「不换模」；Mac 16GB 可上大模型；多语优先 Gemma4-E4B。 | — | — | `l0Config.js` · `l2PromptAdapter.js` · `l2Persona.js` | 2026-09-16 |
