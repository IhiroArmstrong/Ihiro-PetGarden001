| Confide · 回复后自动滚到底（Share 后见答句） | UI可见 | 待人工测试 | **仅 Electron 宽屏 Confide（`desktop:dev` · L2 ready）。** **主路径**：Idle → Confide ready → 面板内已有 status 行 + 输入框 → 发任意闲聊（例：`胖墩儿是干啥的？` / `What does the little glutton do?`）→ **0–1s** 内见 user 行 + reply → **无须手动滚**即可读到完整答句（`[data-testid=confide-to-yin-reply]` 在视口内）。**负例**：不得把输入框留在视口、答句藏在 `overflow:auto` 底部。**375/Web**：无 generate 时若内容短仍应可读。**回流**：连续第二问仍自动滚到最新 reply。自动化：`textEntryOverlayDismiss.test.js`（`_scrollReplyIntoView` 契约）。 | **2026-09-09 用户书面**（Electron 5173 · tip `ecb395d2` · 日语）：问「胖墩儿是干啥的？」答「お腹が太い人が仕事する職業。」内容合理，但录入后须滚到最下端才见答句。**工作流根因**：`overflow:auto` 无 `_showReply` 滚底。**2026-09-09 本地修**：`_scrollReplyIntoView` → `scrollTop = scrollHeight`；待复测。 | post-v1 | `fix/electron-qa-hud-import-ear-locale` | `desktop:dev` · `#confide-to-yin-card` · `[data-testid=confide-to-yin-reply]` | 2026-09-09 |
