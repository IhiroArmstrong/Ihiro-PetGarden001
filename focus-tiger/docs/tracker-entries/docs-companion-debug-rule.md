# docs/companion-debug-rule · 2026-08-23

| 调试本地 AI companion · Agent 规则（companion-debug） | 纯后端 | 仅单元测试覆盖 | 规则 `.cursor/rules/focus-tiger-companion-debug.mdc`：先定点、限读最近一条日志、测试循环上限、简单调试不升档。引入时 alwaysApply；2026-08-24 起改为 glob（见 `docs-cursor-rules-glob-batch1`）。`npm run docs:check` 含 `rules:doc-check`。无用户路径。 | — | — | — | `.cursor/rules/focus-tiger-companion-debug.mdc` · `RULES_INDEX` `companion-debug` | 2026-08-23 |
