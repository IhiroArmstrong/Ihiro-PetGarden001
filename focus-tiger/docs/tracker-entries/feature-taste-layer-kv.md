| Taste layer weights + Honesty 30 → KV (`TASTE_LAYER_KV`) | 纯后端 | 待人工测试 | **主路径（`?product=1` · 配 `VITE_CLOUD_API_BASE_URL`）**：硬刷新后欢迎/Idle 后 `__tasteLayer.status()` → weights + `honestyLongMinMinutes: 30`（KV 空或未部署时仍冻表）。**对照**：`?tasteLayer=0` → 不拉取。**慢网/失败**：不得挡 Sit；仍用冻表。**分叉验收**（须 deploy + 创建 `TASTE_LAYER_KV` + KV 写入 Honesty 20 后）：`emotionKeyForHonestyDuration(20)` → `goldenHaloPalms`；29 → `mindfulAcknowledge`。**禁止**：把现网分叉写回 `tasteLayerFreeze.ts` / `sceneAnimationDispatcher.js`。**分列**：① Idle 呼吸不闪；② overlay 开着 Arrival/Honesty 叠化不闪（`RB-20260820-L330`）。自动化：`tasteLayerConfigKv.test.ts` · `tasteLayerOverlay.test.js` · `tasteLayerSync.test.js` · `sceneAnimationDispatcher.test.js`。 | — | — | — | `feature/taste-layer-kv` · Worker 须口令「部署」+ `TASTE_LAYER_KV` namespace 创建 | 2026-09-12 |
