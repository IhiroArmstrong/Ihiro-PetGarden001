# fix/flower-blow-sequence-guard

| 冷启动吹花须整段播完（气泡先走、毛玻璃卡后到） | UI/交互 | 待人工测试 | **清库** Safari `?product=1`：清 `focus-tiger.flower-welcome.v1` + `cold-start-goal-seen.v1` + session choice → 硬刷新。① 白玉气泡约 **3s** 后淡出，此时**吹花仍在演**（花瓣继续被吹散），**不得**在气泡消失瞬间硬叠回打坐；② 吹散演完后约 1s 叠化回闭目坐禅，**再**出四选毛玻璃卡（比旧版晚约 2.5s），阿寅在卡后仍可见；③ 点 Study or work → 计时页阿寅仍在。**回流**：同日再刷不吹花，四选卡（若未选过）照旧能出；`?flowerWelcome=0` 走书/点头路径，首张卡不得被永久挡住。**保护面**：清晨苏醒（`MORNING_WAKE`）与支付致谢首帧仍须播完回 idle；摸摸提示在吹花期间不得亮、播完后可亮。自动化：`overlaySlotArbitration.test.js` C6b（序列在播时首张卡须 defer）+ `e2e/flower-welcome.spec.js`「plays out after the bubble leaves」（气泡收起时可见帧须仍是 `conjure-flowers-blow-away`，之后须回 `idle-breathing` 且四选卡可见）。 | 2026-09-12：吹花动画看不清——气泡 4.1s 走时程序当成第一幕演完，提前叠回打坐，吹散尾段没播；毛玻璃卡也压在未播完的吹花上。 | — | 本回合改判据（序列播完）+ 门闩；序列观感须人工 | `http://127.0.0.1:5174/?product=1`（本旁支 Vite，勿抢 QA 5173） | 2026-09-12 |
