# fix/desktop-preload-cjs

| Electron preload CJS（桌面壳桥） | UI可见 | 待人工测试 | **仅 Electron 桌面窗。** 关现在跑着的 Electron 后，在本旁支 `cd /Users/armstronghesapplelaptop/Downloads/Zen-tiger-Pet-garden001-wt-desktop-preload-cjs/focus-tiger` → 用 trim 后的 Electron 二进制开窗（或修完 `desktop:dev` 后再用该脚本）。Idle 宽窗 → 点 `⋯` → **Practice & moments** 最末行须见 **Confide to Yin**。DevTools Console 贴：`({ isDesktop: Boolean(window.desktopShell?.isDesktop), hasCompanion: Boolean(window.desktopShell?.companion) })` → 两者须为 **true**；Console **不得**再出现 `Unable to load preload script` / `Cannot use import statement outside a module`。**回流**：关 `⋯` 再开，该行仍在。**Safari :5173**：仍无这一行（网页挂载仍关）。**禁止**第四/第五卡 Checkout。 | **2026-08-22 用户书面（图）**：顶栏是 Electron，`⋯` 无 Confide to Yin；Console `isDesktop: false` + preload `import` SyntaxError。 | — | — | Electron 桌面窗 · `#ft-wide-more-menu [data-testid=idle-confide-desktop]` | 2026-08-22 |
