| ? 简介卡「重新选择今日方向」链 | UI/交互 | 待人工测试 | seen 已写（`dismissColdStartOverlay` 或首跑选过）→ 宽 `#onboarding-hint-help` / 窄 `#ft-narrow-help-btn` → 简介卡见 `data-testid=onboarding-purpose-today-direction` → 点链 → 简介关 → 四选卡开。首跑 seen 未写 → 链 hidden。e2e：`today-direction-help-entry.spec.js`。 | — | — | — | `OnboardingHintsUI.js` · `main.js` `openTodayDirectionManual` | 2026-09-24 |
| 四选卡选项版本轻横幅（方案 A） | UI/交互 | 待人工测试 | seen 已写 + `options-seen` < `OPTIONS_VERSION`（e2e 可设 `'0'` 后 `__todayDirectionOptionsBanner.sync()`）→ Idle 顶栏见横幅。Dismiss → 不再出现；CTA → 开四选卡。首跑 unseen → 无横幅。迁移：老用户仅有 seen 无 options key → 启动时自动 mark 当前版本、不误弹。e2e：`today-direction-options-refresh.spec.js`。 | — | — | — | `coldStartGoalGate.js` · `TodayDirectionOptionsBannerUI.js` · `main.js` | 2026-09-24 |
