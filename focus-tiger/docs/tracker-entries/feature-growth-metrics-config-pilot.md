| Growth metrics remote params pilot (`dailyScoreCapMinutes`) | 纯后端 | 待人工测试 | **主路径（`?product=1` · 配 `VITE_CLOUD_API_BASE_URL`）**：硬刷新后 Idle 约 12s（或 welcome 后）DevTools `__growthMetrics.status()` → `{ growthMetrics: true, dailyScoreCapMinutes: 180 }`（KV 空或未部署时仍 180）。**对照**：`?growthMetrics=0` → 不拉取；`growthMetrics: false`。**慢网/失败**：不得挡 Sit；仍用冻表 180。**分叉验收**（须 deploy + KV 写入 240 后）：status 显示 240；单日 score 封顶按 240 计（花朵仍不封顶）。**禁止**：把现网 240 写回 `scoreDailyCap.js`。自动化：`growthMetricsConfigOverlay.test.js` · `growthMetricsConfigSync.test.js` · `scoreDailyCap.test.js` · cloud `growthMetricsConfigKv.test.ts`。 | — | — | — | `feature/growth-metrics-config-pilot` · Worker 须口令「部署」+ `GROWTH_METRICS_KV` 绑定 | 2026-09-10 |
