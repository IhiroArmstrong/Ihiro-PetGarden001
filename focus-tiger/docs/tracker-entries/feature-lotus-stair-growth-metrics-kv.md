| Lotus stair coefficients → GROWTH_METRICS_KV | 纯后端 | 待人工测试 | **主路径（`?product=1` · 配 `VITE_CLOUD_API_BASE_URL`）**：硬刷新后 Idle 约 12s → DevTools `__growthMetrics.status()` → 含 `lotusFirstBloomMinutes: 25` 等 5 个莲花系数（KV 未分叉时与冻表一致）。**对照**：`?growthMetrics=0` → 不拉取。**分叉验收**（须 deploy + KV 写入后）：例 `lotusFirstBloomMinutes: 20` → `thresholdMinutesForBloom(1)` 行为变为 20 分钟首朵（可用 `qaLotusBlooms` 观察）。**慢网/失败**：不得挡 Sit；仍用冻表 25/25/5/45/12。自动化：`growthMetricsConfigKv.test.ts` · `growthMetricsConfigOverlay.test.js` · `growthMetricsConfigSync.test.js` · `lotusPondMath.test.js`。 | — | — | — | `feature/lotus-stair-growth-metrics-kv` · Worker 须口令「部署」后才含新字段 | 2026-09-12 |
