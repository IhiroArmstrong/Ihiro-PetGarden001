# feature/calm-action-reflect-overlay

| Calm Action Reflect 句包 overlay（`/api/calm-action-copy` schema 2） | 纯后端 + UI文案 | 待人工测试 | **主路径（`?product=1` · 配 `VITE_CLOUD_API_BASE_URL`）**：硬刷新后 `__tasteLayer.status()` 含 `calmActionCopy: true`。**Reflect**：Breath 完成 → Rise → Reflection 三问阶段见 `calm-action-reflect-line`；完成页 Daily Wisdom 渐显前句与本地冻表一致（冻表相同则不另存 overlay）。**对照**：`?tasteLayer=0` → 卡仍正常。**慢网**：失败不得挡 Skip/Continue / Daily Wisdom。**回流**：同日 Reflect 仍日锁。自动化：`tasteLayerOverlay.test.js` · `tasteLayerSync.test.js` · `CalmActionReflectStore.test.js` · cloud `tasteLayerFreeze.test.ts`。**完整用户链路无 e2e**（须人工）。 | — | — | — | `feature/calm-action-reflect-overlay` · Worker schema 2 须口令「部署」才上现网 | 2026-09-09 |
