# docs/cursor-rules-glob-batch1 · 2026-08-24

| Cursor 规则第一批 alwaysApply → glob | 纯后端 | 仅单元测试覆盖 | `companion-debug` / `background-network` / `interaction-feedback` / `feature-conflict-review` 四份 `.mdc` 改为 `alwaysApply: false` + globs。打开匹配路径时注入；口令可靠 description 挂载。`RULES_INDEX` 清单已标明非 alwaysApply。regression-lock / docs 未拆。`npm run docs:check` 含 `rules:doc-check`。无用户路径。 | — | — | — | `.cursor/rules/focus-tiger-companion-debug.mdc` · `focus-tiger-background-network.mdc` · `focus-tiger-interaction-feedback.mdc` · `focus-tiger-feature-conflict-review.mdc` · `RULES_INDEX` | 2026-08-24 |
