| YPE L2 Worker（ingest / 关即删 / Pack） | UI可见 + 后端 | 待人工测试 | **主路径**：Privacy 第四条 ON → Idle 约 2.5s 后 DevTools 见 `POST /api/ype-personalization-ingest`（非 Focusing/Arrival 叠层）→ 样本≥10 次完成时响应含 `pack` → 本机 `focus-tiger.ype-personalization-pack.v1` 写入。**关**：取消勾选 → 0–1s checkbox 关 → `POST /api/ype-personalization-delete`（有网）→ Pack key 清除；**不得** toast「云端已删」。**无网关**：checkbox 仍关，Pack 本地丢弃，delete 排队。**Sit/Confide**：全程不阻塞。**禁止**：Pack 含 rankHint / 非空 patternInsights。 | — | — | — | Worker 部署须 `YPE_PERSONALIZATION_KV` | 2026-08-26 |
