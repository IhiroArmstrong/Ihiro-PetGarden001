| 功能 | 类型 | 状态 | 测试步骤 | 用户反馈 | 严重度 | 处理承诺 | 本地访问 | 日期 |
|---|---|---|---|---|---|---|---|---|
| Confide 在场模板生产分叉验证（`CONFIDE_COMPANION_PRESENCE` · Worker only） | UI文案 | 待人工测试 | **前置**：生产 Worker Redeploy（2026-09-03 · 本旁支）。**自动化已锁**：`curl /api/confide-copy` en → `CONFIDE_COMPANION_PRESENCE` = `Yin is still here. We can stay like this — nothing needs to begin.`；`tasteConfideCopyOverlayMatchesLocalFreeze` = false（分叉分支）。**人工主路径（Electron 宽屏 Confide · 英文 UI · `?product=1`）**：① 硬刷新；② `__tasteLayer.status().confideCopy` → true；③ ⋯ → Confide → 发 `Just stay with me.` → **0–1 秒内** `data-source=companion_presence`，回复须为 **Yin is still here. We can stay like this — nothing needs to begin.**（不是 `Yin is here. We can stay like this — no need to begin.`）。**对照**：`?tasteLayer=0` → 仍本地冻表句。**日文 UI**：`CONFIDE_COMPANION_PRESENCE` 仍冻表日文句。**boundary / 情绪桶**：未分叉，句文与冻表一致。**回流**：关卡再开同句不变。**禁止**：git locale 冻表被改（本刀只分叉 Worker）。 | — | — | — | `https://focus-tiger-cloud.ihiro.workers.dev` · `chore/confide-prod-fork-verify` | 2026-09-03 |
