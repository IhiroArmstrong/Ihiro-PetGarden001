| Support Checkout 第四卡 Pro + 第五卡 Lifetime AI Add-on | UI可见 | 待人工测试 | **主路径（Safari `?product=1` 或 Electron）**：Support FAB → 见 **第四卡 Focus Tiger Pro**（非 Lifetime、未购 Pro 时）或 **第五卡 AI Companion Add-on**（已 Lifetime、未加购时；两卡互斥展示）→ CTA **0–1 秒内** disabled → Stripe Test Mode Checkout → 回跳 `pro_session=` 或 `companion_addon_session=` → 致谢动效。**Pro**：`localStorage` entitlement `planId=focus-tiger-pro`；Electron 宽屏 ⋯ 出现 Confide 生成（须 `companionEntitled` 或已购）。**Add-on**：`focus-tiger.companion-entitlement.v1` active；同上。**回流**：Lifetime 后 Sanctuary 卡保留并淡化（`is-settled`、CTA disabled）；加购卡出现。加购付完后第五卡**保留并淡化**（不得消失）。Membership-only 仍无生成。**禁止**：只接第四卡不接第五卡。自动化：`companionEntitlement.test.js` + cloud route 单测（若有）。Worker 须 redeploy 后测结账。 | **2026-09-01 用户书面**：Lifetime 付完 Support 会跳出 Local AI add-on 卡（OK）；再点加购付完后该卡消失（不对，应淡化保留）。Lifetime 付完 Sanctuary 卡应淡化且按钮不可点。网页无 Local AI 须提醒付费用户。 | — | — | Support `#yin-support-pro-cta` · `#yin-support-companion-addon-cta` · Stripe Test | 2026-08-25 |
