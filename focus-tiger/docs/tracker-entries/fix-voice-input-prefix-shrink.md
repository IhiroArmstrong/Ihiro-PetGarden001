| Voice Input · prefix shrink still shows truncation hint | UI可见 | 待人工测试 | **不要用 5173。** 停掉旧的 `desktop:dev`，在本旁支 `cd focus-tiger && npm run desktop:dev`（宽屏 Electron）→ Confide → Speak to type → 连续说一段明显超过 280 字的话 → Stop。若听写只带回后半段，框里应仍留下更早的长稿（再按 280 字留下开头），并出现琥珀色截断提示。短句正常替换、不出现该提示。回流：再点 Speak again 说短句，提示应消失、框内只留新句。单测：`voiceInputBridge.test.js` | **2026-09-26**：超 280 字长句没有琥珀色提示，前半段丢失。后一次识别整句覆盖，回来的已短于 280，截断检查没机会触发。 | release-blocker | 短很多的后半段不覆盖更长的中间稿，并沿用原截断提示 | `macos-speech-helper.swift` · `voiceInputBridge.js` · `VoiceInputChrome.js` | 2026-09-26 |
