| Support 结账错误内联提示（NA4/NA5 · P4） | UI可见 | 待人工测试 | **主路径**：`?product=1` Idle → 右上 Support FAB → 点 **Focus Tiger Pro** 或 **AI Companion Add-on** CTA（须未结算、卡可见）。**NA4**：本构建未配云 API（本地无 `VITE_CLOUD_API_BASE_URL` 或 Electron 未接云）→ **0–1 秒内**对应卡 CTA 下方出现一行内联错误文案（`[data-testid=yin-support-pro-checkout-status]` 或 `yin-support-companion-addon-checkout-status`），**不得**弹系统 `alert`；Support 模态保持打开。**NA5**：云 API 已配但 Session 创建失败（可临时断网或 mock 失败）→ 同上内联文案，模态仍开。**成功对照**：正常 Checkout 仍跳转 Stripe，模态在打开外链前收起。**回流**：关 Support 再开 → 内联错误清空；换另一卡 CTA 再试仍正常。**375**：内联文案在 CTA 下方可读、不挡其它卡。 | — | — | — | `http://127.0.0.1:5173/?product=1` · `#yin-support-modal` · `[data-testid=yin-support-pro-checkout-status]` | 2026-09-15 |
