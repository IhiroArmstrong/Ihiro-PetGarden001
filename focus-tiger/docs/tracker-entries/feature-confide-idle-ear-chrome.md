# feature/confide-idle-ear-chrome

| 功能 | 类型 | 状态 | 测试步骤 | 用户反馈 | 严重度 | 处理承诺 | 本地访问路径 | 最后更新日期 |
|---|---|---|---|---|---|---|---|---|
| Idle 倾听耳第二入口（向阿寅倾诉） | UI可见 | 待人工测试 | **主路径（宽屏 ≥480 · Electron 非低配，或 Web `?product=1&confide=1`）**：Idle → 左上角白底耳朵钮 `#confide-ear-chrome` 默认约 **30%** 透明、不抢中央 → 鼠标悬停或键盘 focus **0–1 秒内**升到约 **80%**，右侧玻璃短句 `Pour out your heart to Yin` / 「向阿寅倾诉」（`#confide-ear-chrome-tip`；**不是**浏览器原生 title）→ 再点：钮按压缩 + `#confide-to-yin-card` 淡入；卡标题仍是 Confide to Yin。**窄屏 ≤479**：顶栏 ActionBar 在 `?` 右侧见 `#ft-narrow-confide-btn`（宽屏左上耳须消失；顶栏内不做 30%+Tooltip，以免和 `?` / ♪ 并排发灰）；点开同一张卡。**菜单仍在**：⋯ / 抽屉「向阿寅倾诉」仍可开同一卡。**回流**：Close / Esc 关卡后再点耳；Sit→⚡→Rise→Skip Reflection 回 Idle 后耳须仍在（闸开时）；Focusing / Arrival / Reflection 开着耳须隐藏，点阿寅仍是 Recover / 摸头不是倾诉。**闸关（Web 默认无 `?confide=1`）**：耳与菜单行都不见。**375**：耳在 ActionBar 内，不挡 Sit / 三球。**触控宽屏**（无 hover）：耳约 55% 以免找不到。**分列观感**：①默认极淡；②悬停透明度+短句；③按下即缩；④卡 220ms 淡入；⑤Focusing 无耳。自动化：`shouldShowConfideEarChrome` + `ConfideEarChromeUI.test.js` + NarrowIdleShell 契约。完整用户链路无新 e2e（须人工）。 | — | — | — | `http://127.0.0.1:5173/?product=1&confide=1` · `#confide-ear-chrome` · `#ft-narrow-confide-btn` | 2026-08-22 |
