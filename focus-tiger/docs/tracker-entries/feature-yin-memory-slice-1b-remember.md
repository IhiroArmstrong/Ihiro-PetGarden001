| Yin Personal Memory Slice 1b（Remember 管道） | 纯后端/逻辑 | 待人工测试 | **Electron 宽屏**：`npm run dev:desktop` → Confide → 首次 Consent **Allow**（若尚未决策）→ 发送 unmatched 且可抽取句，例：`I prefer quiet, short reflections.` → L3 成功回复后查 `userData/companion-l2/yin-personal-memory.json`：`memories` 有 1 条 `active`，`sourceRoute=confide_fallback`，`status=active`。**回流**：同 rule 再发一次（如 Monday crowded）→ 条目数不增、`confidence` 升档。**负例**：Consent Not now → 仍 L3 但 `memories` 空；`I'm tired`（tired 桶）/ `How long have I practiced?` / 危机句 → 不写 memory。**Web/375**：无 bridge，行为不变。 | — | — | — | — | 2026-08-25 |
