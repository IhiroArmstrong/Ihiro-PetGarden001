| Companion idle unload（关 Confide / shell hidden · 60s 宽限） | UI可见 | 待人工测试 | **仅 Electron 非低配宽屏。** ① Confide ready 后关面板 → **60s 内**重开 Confide → status 仍 ready（不必重下模型）。② 关面板 **>60s** → 再开须重新 loading（或 ensure）。③ 模型 loaded 时收托盘/切 App **>60s** → RSS 应回落（对照 spike unload ~1.27GB）。④ **60s 内**恢复窗口 → 不 unload。⑤ Sit→Focusing 仍**立即** unload（不套宽限）。自动化：`desktopCompanionUnloadSchedule.test.js` · `desktopCompanionL1.test.js` 接线锚。 | — | — | — | `task-companion-model-unload-background-confide.md` | 2026-09-06 |
