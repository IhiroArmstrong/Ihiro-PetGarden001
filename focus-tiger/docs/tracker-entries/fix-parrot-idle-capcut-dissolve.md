| 鹦鹉→Idle 可见叠化（CapCut 门闩） | UI可见 | 待人工测试 | **修复**：`SpriteSequencePlayer.resolveCapCutCrossFadeGate` — overlay 被误藏（opacity 0）但上一帧仍在时，先 `_show()` 再开 1s 叠化；**不是** opacity 0 硬叠。`_oneShotPlayOpts` 默认 `holdLastFrame: true`；manifest `parrotEarVisit.holdLastFrame: true`。**测**：① `?product=1` 硬刷新 → 提醒横幅/里程碑触发鹦鹉 → 播完回 Idle **无硬闪**、约 1s 叠化；② 调试面板「鹦鹉耳边造访」同路径；③ 吹花结束后补播鹦鹉（场景 E12 组合）回落仍无闪白。**自动化**：`SpriteSequencePlayer.test.js` `resolveCapCutCrossFadeGate` + `EmotionController.test.js` parrot onComplete→idle CapCut。**禁止**只验「有 1000ms 字段」。 | **2026-09-15 用户书面**：鹦鹉回 Idle 仍硬闪、提过不止一次 | — | — | `?product=1` · `#emotion-debug-ui` · 场景 E12 | 2026-09-16 |
