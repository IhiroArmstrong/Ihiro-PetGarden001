# Focus Tiger · Cloudflare Worker env template (local wrangler only)
#
# Do NOT put production Secret Keys in the Vite client (.env*).
# Worker secrets: `npx wrangler secret put <NAME>` (or CF dashboard).
# GitHub Actions: repo Settings → Secrets and variables → Actions
#   (only when a workflow actually references secrets.<NAME>).
#
# Buy Yin a Tea · Tip Jar secrets (put via wrangler; never commit values):
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
#
# Entitlement restore OTP (Sanctuary + Membership; not Tip):
# RESEND_API_KEY=
# RESTORE_OTP_PEPPER=
# RESEND_FROM=Yin <restore@twinsology.com>   # non-secret; also in wrangler.jsonc vars
# NEWSLETTER_FROM=Yin <hello@twinsology.com> # welcome mail; falls back to RESEND_FROM
#
# NEWSLETTER_KV (created 2026-08-13; ids in wrangler.jsonc):
#   id         = baeb661cb8f2450ab4a87d6f23af6896
#   preview_id = 8e13fe05705841c9939c3164bfb9a3bd
#
# OTP_KV: create namespaces then replace placeholders in wrangler.jsonc
#   npx wrangler kv namespace create OTP_KV
#   npx wrangler kv namespace create OTP_KV --preview
#
# Non-secret vars live in wrangler.jsonc `vars` (or Dashboard):
# STRIPE_PRICE_ID=price_...              # Tip one-time
# STRIPE_SANCTUARY_PRICE_ID=price_...    # Sanctuary Lifetime one-time
# STRIPE_MEMBERSHIP_PRICE_ID=price_...   # Yin Membership recurring
# MEMBERSHIP_CHECKOUT_SUCCESS_URL=http://127.0.0.1:5173/?product=1&membership_session={CHECKOUT_SESSION_ID}
# MEMBERSHIP_CHECKOUT_CANCEL_URL=http://127.0.0.1:5173/?product=1&membership=cancel
# CHECKOUT_SUCCESS_URL=http://127.0.0.1:5173/?product=1&tip=1
# CHECKOUT_CANCEL_URL=http://127.0.0.1:5173/?product=1&tip=cancel
# ALLOWED_ORIGIN=http://127.0.0.1:5173
#
# Example future names (do not invent values here):
# CLOUD_API_SIGNING_KEY=
# RATE_LIMIT_KV=
