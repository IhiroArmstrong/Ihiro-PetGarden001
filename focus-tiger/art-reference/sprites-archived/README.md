# Archived sprites

Frames moved out of `public/sprites/` so they are no longer served or
listed as runtime candidates.

| Directory | Archived | Reason |
|---|---|---|
| `breath-halo-expand/` | 2026-07-20 | Replaced by `breath-halo-hq` (16-frame HQ) as MilestoneGlow alternate |

Do not re-copy into `public/sprites/` without updating `spriteManifest.js`
and `EMOTION_BIBLE.md`.
