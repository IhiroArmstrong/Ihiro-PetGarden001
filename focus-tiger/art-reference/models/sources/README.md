# 3D source models (local)

High-resolution / raw GLB sources for authoring. Optimized runtime copies live in `focus-tiger/public/models/`.

Large source files here are intentionally **not** committed (see repo root `.gitignore`). Keep them on disk for re-export; sync via other backup if needed.
