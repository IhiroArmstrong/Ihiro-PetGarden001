---
description: When listing multiple options, always state which one you consider most reasonable
alwaysApply: true
---

# Focus Tiger · 给选项时必须给「最合理项」

目的：禁止把决策摊成「或者这个、或者那个」的并列菜单，让用户替 Agent 做完分析。

索引：`RULES_INDEX.md` → `recommend-most-reasonable`。与 N14 / N14a 并列：N14 管清单位置；N14a 管伪选项标 `（不合理）`；**本条管有真开放方案时必须表态**。

## 强制（本会话）

只要回复里出现 **≥2 个可执行方案 / 分支 / 默认**（含「或者 A 或者 B」、对照表、待你决定里多条开放项），就必须同时写出：

1. **我认为最合理的是：…**（只选一项；可写在段首，不必埋在菜单末尾）
2. **为什么**（1～3 句）
3. 其余方案用一句说明为何较弱 / 仅在何种情况才用——**不要**把它们写成同等待选

用户仍可推翻这一项；本条 **不** 代替拍板，只禁止 Agent 不表态。

## 不做

- 只给并列菜单、不给倾向
- 用「都可以 / 看你喜欢 / 两种都行」结束分析
- 把本条理解成「替用户点 Merge / 改 GitHub 设置」——做不到的动作仍须写进「待你决定」，但倾向仍要写明

仅一条合规路径、或 N14a 已全部标 `（不合理）` → 不必硬造第二方案。
