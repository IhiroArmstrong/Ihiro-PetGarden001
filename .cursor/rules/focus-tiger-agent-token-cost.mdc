---
description: Agent Token Cost——跑测试/e2e/CI、大范围读仓、子 Agent 相关任务前须 Read 全文
alwaysApply: false
---

# Focus Tiger · Agent Token Cost（控 Fast Request · 按需层）

> **加载时机**：跑测试/e2e、读 CI 结果、预计多文件搜索、或用户提到 token 成本时。测试输出卫生摘要亦在 `focus-tiger-core.mdc`。

目的：阻止 Agent 自动多轮递归、并行子 Agent、长 CI 守候、无限制上下文读取、默认走高价模型档位，瞬间吞掉数十次 Fast Request 或天量 token。

索引：`RULES_INDEX.md` → `agent-token-cost`（工具调用预算分档另见 `agent-tool-budget`；本地 e2e 硬顶另见 `e2e-local-budget`）。硬闸门另见 `.cursor/hooks.json`（`subagentStart` deny + full-e2e shell ask + **`gate-local-heavy-e2e` deny** + **`tool_budget.sh` 分档硬顶**；**IDE Browser MCP deny** 见 `browser-energy`）。`PROCESS.md`「本地 Cursor 能耗」有同款速查表（只指路，不复述全文）。

## 速查（Merged · 新会话 · 模型一行）

**PR 合入后（`Merged` / 「PR 合了」/ `#NNN` 已合）→ 必须新开 Chat。** 禁止在同一会话里接下一任务、验收或 Git 同步——旧会话的工具返回与规则前缀会在**每一轮**重复计费；单轮 500万–650万 token 多由此而来。

**新会话第一条消息末尾附一行模型（用户口吻；Agent 可提示、不得擅自切换）：**

| 任务类型 | 推荐一行 |
|---|---|
| 机械 Git / PR / QA 树同步 / 状态确认 | `Cursor Model: Composer 2.5` |
| 常规实现、多文件改代码、局部调试 | `Cursor Model: Composer 2.5 / Fast OFF` |
| 跨模块架构、疑难推理、安全边界设计 | `Cursor Model: Grok 4.6 / Fast OFF` |

**例外**：用户当回合书面「就在这里做」→ 方可继续当前会话（仍建议换便宜模型）。

## 工具调用预算分档（agent-tool-budget · 可执行）

> **执行层**：`.cursor/hooks/session_gate.sh`（`beforeSubmitPrompt` 选档 + 重置）+ `.cursor/hooks/tool_budget.sh`（`preToolUse` **只计探索类**）。数字 SSOT：`.cursor/hooks/config.json`。

### 计什么（强制）

硬顶打在**浪费的探索**上，不打断收尾干活：

| 计入探索预算 | 不计入（硬顶也不拦） |
|---|---|
| `Grep` / `Glob` / `codebase_search` / WebSearch / WebFetch / SearchConversations / Task | `StrReplace` / `Write` / `Delete` |
| `Read` 违反 `source-read-granularity` 阈值（见下） | 合规片段 `Read`（≤`max_productive_read_span` 行）与 **&lt;200 行**短文件整读 |
| Shell 里的 `rg` / `grep` / `find` / `git grep` | `git` / `gh`（非 grep）、约定的一次 `test:smoke` / `node --test` |

**`Read` 计次与 `source-read-granularity` 对齐**（数字 SSOT：`config.json` 的 `short_file_lines` / `source_read_large_lines` / `max_productive_read_span`，与 `focus-tiger-source-read-granularity.mdc` 同阈）：

- **&lt;200 行**：任意 `Read` 不计次。
- **≥200 行且无 `offset`/`limit`**：整读，**计次**。
- **≥200 行且带 `offset`/`limit`**：若本次读取跨度 **&gt;200 行**（含「假定点大 `limit`」），**仍计次**；合规小窗口片段读不计次。

**连续探索软停**（与累计软/硬顶**并列**，不替代硬顶）：连续 **8** 次探索类调用且其间无 `StrReplace`/`Write` → hook 先 **ask 软停**；`StrReplace`/`Write` 会清零连续计数。累计硬顶仍保留。

软上限 / 连续探索软停到达时：**先口头汇报完整探索快照**（已读文件 / 已确认 / 已排除假设）；改文件与 git 仍可走。硬顶到达时：**只 deny 再探索**，且须附完整探索快照；禁止因此新开 Chat。

**「大任务」开工纪律**：首条回复须先列 **预期文件/函数清单**，再只读审计；勿立刻改代码（`session_gate.sh` 会提醒）。跨目录 / monorepo 任务，首条口令还须带 **`@folder` 目录白名单**（或等效「只碰 `focus-tiger/src/…`」书面范围）——**禁止**无范围全仓 `Grep`/`Glob` 摸底。口令范例：`大任务 只碰 focus-tiger/src/journey/ 和 focus-tiger/src/core/，先列文件地图再开工`。

### 三档表 + 口令

| 档位 | 用户口令（消息中含） | 软上限 | 硬上限 | 典型场景 |
|---|---|---|---|---|
| `qa` | 默认（随口问 / 核对 / 首条无口令） | 22 | **28** | 状态确认、是否已实现、短问答 |
| `impl` | **「开工」** | 40 | **64** | 局部/少文件实现；软上限=先停口头汇报 |
| `large` | **「大任务」** | 64 | **80** | **跨模块 + 单测 + PR**（含 Yin Evolution P0 这类刀） |

- **首条消息**无口令 → 自动 `qa`；同会话后续消息**保持**当前档位，直至口令升档或「继续」重置。
- **「开工」** / **「大任务」** → 写入对应档位并**重置**探索计数。
- **「继续」**（可附任务名，如「继续 P0」）→ **继承**当前档位（尚无档位时默认 `impl`）、写 `continue_resume` 标记、**重置**探索计数。Agent 应**少勘探、直接续写**（见下节）。
- 跨模块、新 core/UI/locale、还要单测并开 PR → 第一条用 **「大任务」**，不要用「开工」硬扛。Agent 若判断属此类而用户只发了「开工」，须在开工回复里请补发「大任务」（**禁止**擅自改档）。

### 中断恢复（「继续」纪律 · 只留本 Chat）

硬顶或软上限之后：**只在被打断的同一 Chat 发「继续 &lt;任务&gt;」**。**禁止**为此 New Agent / 新开 Chat（会把规则前缀与全仓搜索再交一遍税）。

**新开 Chat 仅允许**：本会话对应 PR 已合入（见上文 `Merged`），或完全换题。

用户发 **「继续」** 或 **「继续 &lt;任务&gt;」** 后：

1. **继承上下文**：不重复汇报已完成工作；从上次中断点接着做。
2. **跳过已完成勘探**：默认**不再**全仓 `grep` / 大范围 `fetch` / 批量打开 ≥5 个文件；除非用户明确要求复查或代码面已变。
3. **验证类 git 检查不受限**：`git status` / `git log` / `check:branch-freshness` 仍该跑就跑（见下节「验证类」）；**且不计入**探索硬顶。
4. **大文件**：须片段读 → `focus-tiger-source-read-granularity.mdc`（`source-read-granularity`），禁止为「再确认一遍」整文件重读。
5. **测试**：实现类任务 → **收尾跑一次** `test:smoke`（或已约定的单 spec）；失败再针对性读日志。**禁止**每改一小处就跑一遍测试（细则见 `testing-strategy.mdc`「实现类冒烟纪律」）。约定 smoke **不计入**探索硬顶。

硬顶耗尽时 hook **只拦截探索类**，并提示在**本对话**发「继续 &lt;任务&gt;」重置探索计数。Agent **禁止**建议新开 Chat 来续同一任务。

## 验证类 vs 探索类（强制 · 勿把省 token 扩到地面真相）

控 token **只约束探索类 / 重成本调用**。**不约束**验证类地面真相检查。两条规则不互相拉扯：本文件省 token；实现状态以 git 为准见 `focus-tiger-regression-lock.mdc`「分支新鲜度」「地面真相优先级」（`RULES_INDEX` → `branch-freshness`）。

**验证类（几乎零成本；怀疑分支或实现状态时无条件执行，不受下方第 5 / 7 / 11 条「少工具调用」约束）**：

- `git status`、`git branch`、`git log --oneline`（可限制条数，如 `-5` / `-20`）
- `git fetch origin develop` 以及 `npm run check:branch-freshness`（或等价的 behind 计数）
- `git branch --contains <hash>` / `git merge-base --is-ancestor`

这类命令与读取大源码、跑全量测试、开子 Agent **不是一个量级**。**禁止**为「少一次工具调用 / 省 token」跳过它们。漏做会把过期 Brief 当成 git 事实，比多一次 `git log` 贵得多。

**探索类（须精简，见下方第 1–12 条）**：大范围 Grep/Glob、一次打开 ≥5 个文件、整文件读大源码、全量 e2e、子 Agent、轮询长 CI。源码片段读见 `source-read-granularity`。

## 强制（本会话）

1. **禁止 Task / 子 Agent**（含 `explore` / `generalPurpose` / `shell` / bugbot / security-review 等）。默认在**父 Agent**内直接完成。
   - 硬闸：`subagentStart` → `permission: deny`（脚本 `deny-subagent-start.sh`）。
   - 仅当你**当回合书面点名**要开某类子 Agent，且你临时关掉/放行该 hook 时，才可例外。
2. **禁止并行 explore / 批量并行子任务**。一次一线索；不要为「加快」开多个 Task。
3. **禁止轮询长 CI**（全量 e2e、visibility 长跑、`gh run watch`、反复 `gh run view` + 长 `sleep`）。
   - 允许：触发一次（须用户当回合批准）→ 贴 run URL → **结束**。
   - 结果另开短回合查摘要；不要在本回合守 30–120 分钟。
4. **禁止擅自触发全量 e2e**（`gh workflow run` 指向 `focus-tiger-e2e-full` / `test:e2e` 全量）。须用户当回合明确批准（如「批准触发 full e2e」）。
   - 硬闸：`beforeShellExecution` → ask（`gate-full-e2e-dispatch.sh`）。
5. **工具轮数上限**（**仅探索类 / 试错空转**）：可执行硬顶见上文「工具调用预算分档」（`tool_budget.sh` 按 `qa`/`impl`/`large` **只计探索**）。单任务若已接近软上限仍无收敛，先停下来用「待你决定」问用户，禁止为凑绿而空转；硬顶耗尽须在**本 Chat**发「继续」重置探索计数（禁止 New Agent）。**本条不覆盖**上一节验证类 git / freshness 检查——那些几乎零成本，该跑就跑，且 **hook 不计次**。CI/smoke 失败且用户未附日志 → 先 Read `focus-tiger-ci-failure-triage.mdc`（`ci-failure-triage`）；其 Step C **5 轮硬顶**在本场景优先于分档硬顶。UI/交互回归 bug 且用户未给足够锚点（复现 / 怀疑 commit / 怀疑文件）→ 先 Read `focus-tiger-ui-bug-triage.mdc`（`ui-bug-triage`）；其 Step C **5 轮硬顶**在本场景同样优先于分档硬顶。
6. **CI 红 / 合入受阻 / 多文件冲突 · 本地验证预算（强制）**
   适用：PR checks 失败、merge 冲突、或预计 **≥3 文件** / **多轮试错** 才能解的改动。
   - **一开始**就用独立 worktree + 分支 / 既有 PR 分支，**禁止**在主仓或其它任务的 worktree 原地反复试。
   - **先**给「冲突 / 失败分析摘要」（哪些文件、什么类型：语法 / 契约 / e2e 选择器 / 真逻辑），**不要**立刻开改。摘要**必须**先完成 `ci-failure-triage` Step A 的日志提取（见 `focus-tiger-ci-failure-triage.mdc`）；**禁止**无 CI 日志信号的臆测摘要。
   - **问**用户是否要新开 worktree/分支处理（避免试错脏了其它任务）。
   - 改完后：**本地最多 1 轮**确认语法 / 明显逻辑（如 `test:smoke` 或 **`test:e2e:changed --` 单个 spec**）；**最终验收交给 push + CI**（`test:pr-smoke` / 相关 workflow）。禁止多文件 changed / 全量 / 直调长列表（脚本+hook 硬拦；`RUN_E2E_LOCAL=true` 逃生口会打警告）。
7. **上下文范围控制（防止大规模误读）**
   - 单文件粒度见 `focus-tiger-source-read-granularity.mdc`（`RULES_INDEX` → `source-read-granularity`）。
   - **目录白名单**：`开工` / `大任务` 首条宜 `@folder` 或写明子目录范围；Agent 探索须限制在白名单内，不得为「摸清边界」扫全仓。
   - 禁止一次性读取/索引整个仓库或大范围目录树，除非用户当回合明确批准范围。
   - 默认以 `.cursorignore` 为准排除 `node_modules`、构建产物（`dist`/`build`/`.next`/`out`）、大体积媒体资源（音频/视频/图片/字体）、大数据文件；若发现这类文件未被排除，先提醒用户补充 `.cursorignore`，不要直接把它们读入上下文。
   - 涉及跨目录 / 多文件（≥5 个文件）搜索或读取前，先说明预计扫描范围，等待用户确认后再执行，不要一次性拉取大范围上下文。
8. **模型档位纪律（防止默认走高价档位）**
   - 简单编辑、局部调试、格式修正、小范围重构：使用当前默认/轻量模型，不主动建议或假设需要切换到 high 推理档位。
   - 涉及跨文件架构设计、复杂逻辑推理、疑难 bug 排查等确实需要深度推理的任务：可以提示用户「这个任务可能需要更强模型（如 high 档位），是否切换？」，但不得擅自假设已获同意并按更高档位继续执行。
   - 模型档位切换权始终在用户手中，Agent 不得通过工具调用绕过。
9. **长会话收尾卫生（Session Hygiene）· `Merged` 硬断点**
   - **`Merged` / 「PR 合了」/ `#NNN` 已合 = 会话终点**：Agent **必须**提示用户新开 Chat，再处理下一任务（验收、同步 QA、新 Brief、下一 PR）。**禁止**默认在当前会话继续——不是「建议」，是硬断点（见上文「速查」）。
   - 其它简短收尾（如「同步一下 QA」）且会话已跨多话题 / 多轮工具时，同样先提示新开 Chat，并说明历史上下文会线性累积计费。
   - 仅当用户明确回复「就在这里做」或类似确认后，才继续当前会话。
10. **Git / PR / QA 同步类任务的模型路由建议**
   - 机械性流程（分支切换、commit、push、开 PR、QA 树同步、状态确认等）→ 任务开始前提示用户附 **`Cursor Model: Composer 2.5`**（见上文速查表）。
   - 不得擅自假设用户已同意切换模型；模型切换权始终在用户手中。
11. **机械性操作的一次性批量规划**
   - 对于 Git/PR/QA 同步这类多步骤机械操作，执行前先列出完整步骤清单（占坑锁、stash、切分支、commit、push、开 PR 等），一次性说明后再连续执行，不要每完成一步就停下来单独确认一次，避免把一个任务拆成过多**探索/确认**轮次（每多一轮，规则前缀就多计一次 token）。**不是**让你省略清单里的 `git status` / `git log` / freshness：那些验证类命令仍须执行，只是不要拆成「每条命令停下来问一次」。
   - 仅当某一步存在风险判断（如可能冲突、可能覆盖他人改动）时，才单独停下来问用户。

12. **测试输出卫生（强制 · 与 `focus-tiger-core.mdc` 一致）**
   - 任何 `npm run test*` / Playwright / `playwright install` 跑完后：回复只写 pass/fail 数量 + 失败用例名 + 一行报错；**禁止**粘贴完整终端输出（同一对话线程里会随轮次线性累积计费）。
   - 需留档 → 写 `focus-tiger/.tmp/test-output-<timestamp>.log`（或 `/tmp/…`）；排查时用 `Read`  targeted 段，**禁止**让大段日志在多轮上下文里常驻。
   - Shell 工具若返回超大 stdout：只向用户汇报结构化结论，不要把原始块原样复读进回复。

## Settings → Agents → Subagents（须你本人在 Cursor UI 设）

项目文件**无法**写入该 UI 项。请你本地确认：

- **Cursor Settings → Agents → Subagents**
  - Explore subagent model → **Inherit from parent**（或固定便宜模型）
  - 有「禁用 / 少用并行」类选项则打开

与本规则 + hook 叠加：UI 降成本，hook 硬禁 spawn。

## 汇报

触及本主题的收尾「待你知道」可一行写：本回合未起子 Agent / 未轮询长 CI（或说明经批准的一次性触发 + URL）/ 未做大范围上下文读取 / 未擅自切换高档位模型或跳过模型路由建议 / 机械操作已按批量规划一次性执行。
