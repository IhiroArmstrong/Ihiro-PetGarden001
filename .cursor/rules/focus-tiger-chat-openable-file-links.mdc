---
description: 用户可见回复里文件须可点开、可定位——Markdown 链接 + 本机绝对路径；立刻看须 open_resource + Finder
alwaysApply: true
---

# Focus Tiger · 文件须可点开、可定位（强制）

> **SSOT**：`WORKFLOW.md` →「用户可见回复：文件须可点开、可定位」；索引 `RULES_INDEX.md` → `chat-openable-file-links`。

Agent 交付或请用户打开的**任何文件**（仓库内 md / 源码 / 规则 / Brief / 产物等）：

## 回复里必须同时给

1. **Markdown 链接**：`[短名](相对仓库根的路径)`（仓库内文件）。
2. **本机绝对路径**（单独一行，便于复制 / Finder / 终端）：`/Users/…/Zen-tiger-Pet-garden001/focus-tiger/docs/foo.md`。
3. 已推到 GitHub 时再给 **https 网页链接**。

**禁止**只写反引号路径（如 `` `focus-tiger/docs/foo.md` ``）——对话里点不开。

## 要用户立刻看

同时执行：

- `open_resource`（`file://` + 绝对路径）——在编辑器打开；
- `open -R '<绝对路径>'`——在 Finder 定位到该文件。

## 用户要「单独一份」

主动问其一（不要静默假设）：

- 复制到 **Desktop** 或 **Downloads**（问清目标目录）；
- **commit + push** 后给 GitHub 网页链接。

引用代码块不是打开文件的替代。
