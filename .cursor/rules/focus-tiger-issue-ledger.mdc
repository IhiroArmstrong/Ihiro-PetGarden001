---
description: Issue Ledger——记录未闭环 bug/根因扇出/优化建议，或口令「核对问题清单」前须 Read
alwaysApply: false
---

# Focus Tiger · Issue Ledger（问题闭环追踪 · 按需层）

> **加载时机**：发现根因扇出、只分析未修复的 bug、记录优化建议/技术债、新任务命中 ISSUE_LEDGER 未跟进项、或用户说「核对问题清单」时。

目的：防止两类主线丢失——①「发现一个根因影响 N 处，实际只追完第 1 处」；②「记录了一个 bug/优化建议，但从此没了下文」。两者本质是同一个洞（记录≠闭环），共用同一套账本和同一套强制流程，不分开维护两套清单。

## 触发条件

出现以下任一情况时，视为需要落账的「问题」事件：

**A. 根因扇出型**：
- 「这个问题不止出现在这里，同类模式在 XX/YY/ZZ 也存在」
- 「这是架构级/系统性问题，影响范围包括……」
- 「建议以后遇到类似情况，也要检查……」

**B. 独立记录型**：
- 用户报告一个 bug，本回合只分析根因/给出方案，未实际修复落地
- 用户提出一条产品优化建议，本回合只记录/讨论，未排期或未实现
- 审计/调查发现一个已知问题或技术债，未在本回合处理

## 强制动作（本回合内完成，不得推迟到下一会话）

1. **落盘清单**：在 `focus-tiger/docs/ISSUE_LEDGER.md`（不存在则创建，若已有 `ROOT_CAUSE_LEDGER.md` 则重命名迁移过来）追加一条记录，包含：
   - **类型**：`根因扇出` / `独立bug` / `优化建议` / `技术债`
   - 问题描述（一句话）
   - 首次发现时间 + 会话/PR 来源
   - 受影响项/涉及范围（根因扇出型需逐条列出，不许用"等"、"以及类似情况"模糊收尾；独立记录型写清楚具体是什么）
   - 状态：`未跟进` / `跟进中` / `已解决`

2. **本回合未能完全解决时**，必须在「待你决定 / 待你知道」收尾里明确说明：
   - 「本次[做了什么]，ISSUE_LEDGER.md 中该项状态为 [未跟进/跟进中]，[如是扇出型：还有 N 项未跟进]」
   - 不得只汇报已处理部分、对未完成部分只字不提。
   - 不得仅因为"已经分析出根因"或"已经给出方案"就在汇报中暗示问题已解决——根因清楚 ≠ 已修复，方案确认 ≠ 已实现。

3. **新任务开工前**，若任务描述或用户消息提到的模块/文件，命中 `ISSUE_LEDGER.md` 里「未跟进」或「跟进中」状态的某一项，主动提示：「这个和 ISSUE_LEDGER.md 里 [问题X] 是同一类/同一处问题，是否一并处理？」

4. **定期核对提醒**：用户发出口令「核对问题清单」或「产品盘点」/「每周盘点」时，输出 `ISSUE_LEDGER.md` 中所有非「已解决」状态的条目（按类型分组），并 Read `WEEKLY_PRODUCT_BACKLOG_AUDIT.md` 执行 §2 四桶扫描；不做筛选或省略。

## 与现有交接机制的关系

`session-handoff` 规则的交接摘要模板中，若本回合涉及任何 `ISSUE_LEDGER.md` 条目（新增或更新状态），「下一步该做什么」字段必须包含指向对应条目的引用，不能只写业务层面的下一步。

## 汇报

触及本主题的收尾「待你知道」需注明：本回合是否新增或更新了 `ISSUE_LEDGER.md` 条目；若有未闭环项，需一并列出，不得只字不提。
