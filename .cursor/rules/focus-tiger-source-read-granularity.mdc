---
description: 源码读取粒度——大文件先定位再片段读，控上下文 token（接实现类任务或读 focus-tiger/src 时挂载）
alwaysApply: false
---

# Focus Tiger · 源码读取粒度（控上下文 token · 按需层）

> **加载时机**：接实现类任务、读/改 `focus-tiger/src/**` 或 `focus-tiger/cloud/src/**` 前；用户提到 token 成本、大文件、或审计发现整读模式时。仓库/多文件级范围控制见 `focus-tiger-agent-token-cost.mdc` §7；日志限行见 `focus-tiger-companion-debug.mdc` §2。

索引：`RULES_INDEX.md` → `source-read-granularity`。

目的：阻止 Agent 对单个大源码文件无理由整文件 `Read`，使上下文成本与任务实际需要的信息量成正比。

## 阈值（强制）

| 层级 | 阈值 | 行为 |
|------|------|------|
| **短文件** | **< 200 行** | 允许整文件 `Read` |
| **中等文件** | **200–399 行** | 默认片段读；若任务明确只涉及文件头部（imports/常量）或单一已知小函数，可整读 |
| **大文件** | **≥ 400 行** | **禁止无理由整文件 Read**；须先定位再读区间 |
| **超大文件** | **≥ 1000 行** | 同上，且单次 `Read` 窗口建议 **≤ 200 行**（含上下文）；跨函数须多次片段读 |

## 推荐流程（≥ 400 行 · 强制顺序）

当任务已知道目标文件，且文件 **≥ 400 行**：

1. **先定位，不先整读**
   - 已知符号 → `Grep` 函数名 / 类名 / `data-testid` / 错误字符串
   - 只知语义 → `Grep` 关键词或 `Glob` 缩小到 1–3 个候选文件
2. **确定行号区间**
   - 从 Grep 结果取匹配行；必要时 `-B`/`-C` 看 import 与调用方
3. **片段 Read**
   - `Read(path, offset=起行-上下文, limit=目标跨度+2×上下文)`
   - **默认上下文**：±**40 行**；跨模块边界（import、export、class 边界）可扩到 ±**80 行**
4. **仍不够再扩**
   - 沿调用链再 Grep / 再开一个新窗口；**禁止**因「可能有用」回退整文件读
5. **与 `agent-token-cost` §7 衔接**
   - 若片段读仍须打开 **≥5 个文件**，继续遵守 `agent-token-cost` §7——先说明扫描范围

## 例外（允许整文件 Read）

1. **文件 < 200 行**
2. **用户当回合明确要求**整读某文件（如「通读 main.js 做架构评审」）
3. **全文件重构 / 大规模搬移 / 格式化整文件**——改动面明确覆盖大部分内容
4. **首次建立心智模型**（新 Agent 接手模块、写 ARCHITECTURE 级说明），且用户任务性质为「理解整体」而非「改一个函数」；读完后后续编辑仍须片段读
5. **规则与权威文档**：`focus-tiger-core.mdc` 索引要求 **Read 全文** 的 `.mdc` / 指定 `docs/*.md` SSOT
6. **生成物 / 数据表类**（如 `spriteManifest.js`）：若任务是核对全表枚举，允许整读；若只改一项，仍应 Grep 定位

## 禁止事项

- 已知只改一个函数，却对 **≥400 行** 文件无定位直接整读
- 用整文件 Read「顺便看看还有什么」
- 同一回合对同一大文件 **重复整读**（应依赖会话内已读片段或磁盘笔记）
- 用 `Read` 无 `limit` 读取 **≥1000 行** 源码（例外见上）

## 汇报

触及本主题的收尾「待你知道」可一行写：大文件是否片段读、读了哪些行区间。
