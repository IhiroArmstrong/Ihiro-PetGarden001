---
description: After a staged task, or when the user says 生成交接, output a structured session-handoff summary
alwaysApply: true
---

# Focus Tiger · 会话交接（Session Handoff）

**目的**：在单次 Agent 会话 context 变长、或完成一个阶段性任务时，主动生成结构化交接摘要，让下一个 Agent（或同一个 Agent 的新会话）能无缝接上，不依赖人工复述背景。

索引：`RULES_INDEX.md` → `session-handoff`。与口令「批量人工测试」（见 `TEST_TRACKER.md`，`qa-batch-human-test`）同级，都是可随时调用的固定动作型口令；**不**代替 N14，也**不**改变 Git / CI / 关单门禁。

## 强制（本会话）

### 触发时机（Agent 应主动判断，不必等用户开口）

- 完成一个明确的阶段性任务（如一个 Prompt/一个 PR）之后；
- 对话轮次明显变长、或涉及的文件/决策点变多，判断「如果此刻被截断，下一个 agent 接不上」时；
- 用户输入口令 **"生成交接"** 时，立即按下方模板输出，不做其他事。

阶段性任务收尾仍先走完既定 commit / 默认 push+PR / N14，**再**附本摘要。口令「生成交接」时只输出模板，不新开任务、不额外 push。

### 交接摘要固定模板

## 交接摘要 [生成于 {timestamp}]
- 当前分支/worktree：
- 本轮已完成：
- 本轮进行到一半/未完成：
- 下一步该做什么（给下一个 agent 的开工口令，写成可直接粘贴的一段话）：
- 需要人工确认/未拍板的悬而未决项：
- 相关文件/PR 链接：

### 开工口令要求

「下一步该做什么」那一栏必须写成**可以直接复制粘贴进新会话第一句话**的形式，不是笼统的任务描述，而是包含：项目名+分支/worktree+接续到哪个具体任务+参考哪个文档+第一步先做什么（通常是「先只读审计当前状态再继续」）。

### 与已有流程的关系

- 与口令「批量人工测试」（见 `TEST_TRACKER.md`，`qa-batch-human-test`）同级，都是可随时调用的固定动作型口令。
- 交接摘要本身只是摘要，不改变 CI/PR/部署的既定流程（push/PR 仍默认允许，合 develop 仍只要 CI 绿，「已验证」结论仍必须用户亲自人工测过才能下——这些规则不受影响）。
