---
description: Focus Tiger 权威文档索引与产品红线摘要——接实现类任务或改产品语义/情绪/架构/文案前须 Read 全文
alwaysApply: false
---

# 坐禅小老虎 / Focus Tiger · 项目规则（按需层）

> **加载时机**：接实现类任务、改产品代码、或触及情绪/架构/文案前，须 `Read` 本文件全文。常驻 Git/N14/索引见 `focus-tiger-core.mdc`。

代码与产品主目录：`focus-tiger/`。权威全文在 `focus-tiger/docs/`，**本规则仅摘要**；细则冲突时以对应文档正文为准。  
**不要**把持续增长的 `TASKS.md` 全文塞进规则；排期只在接任务时按需查阅。

## 任务开始前

1. 实现前冲突扫描 → `FEATURE_CONFLICT_REVIEW.md`（`RULES_INDEX` → `feature-conflict-review`）；有冲突疑点须等用户拍板。勿在本文件复述三轴。  
2. 确认本次改动是否触碰下方红线 / 架构边界。  
3. 产品定位、范围、品牌措辞相关任务 → 先对照 `PRODUCT_POSITIONING.md`；用户、JTBD、指标、付费或隐私相关任务 → 先对照 `MVP_PRODUCT_DEFINITION.md`；情绪、互动、文案相关任务 → 先对照 `EMOTION_BIBLE.md`。
4. 模块归属不清 → 对照 `ARCHITECTURE.md` + `PROCESS.md` 角色分工。 
5. Prompt 未手写引用文档时，仍以本规则 + 文档路径为准，必要时主动打开对应 md。

## 权威文档地图

| 文档 | 用途 |
|---|---|
| `docs/PRODUCT_POSITIONING.md` | **品牌与产品战略顶层**：核心使命、正念伙伴定位、宁静型游戏化、反馈分级与长期边界；产品语义层级高于 `DESIGN.md` |
| `docs/MVP_PRODUCT_DEFINITION.md` | **MVP 产品策略基线**：首要用户、JTBD、竞争替代品、验证指标、付费假设与隐私承诺；假设不等同于已上线功能 |
| `docs/PRODUCT_MOMENTS.md` | **一天叙事框架（Five Moments）**：Arrive / Focus / Recover / Transition / Reflect；位于定位之下、`TASKS` 之上，不推翻已有实现 |
| `docs/CORE_LOOP.md` | **单次会话七步状态机**：Arrive→Focus→Recover→Reflect→Grow；与 Five Moments 的粒度关系 |
| `docs/ARRIVE_MOMENT_DESIGN.md` | Arrival Practice 交互详规（v2：Notice/Choose 点选为主） |
| `docs/LIGHT_PROGRESSION_DESIGN.md` | 光影物理渐进（2D DOM/CSS）：Arrival 氛围 + Recover 扰动；排除主界面 3D/Shader |
| `docs/PRINCIPLES.md` | **硬性红线**：经济可持续（非学雷锋）、不制造焦虑、永不消失、诚实机制、一次一任务、不超范围、性能与风格 |
| `docs/ARCHITECTURE.md` | 模块边界、主线 **2D PNG 序列**、3D 资产保留给奖励场景、`EmotionController` 桥梁 |
| `docs/YIN_PERSONAL_MEMORY.md` | **阿寅个人记忆架构 V1**（方向锁；≠ 练习云备份 / Journey Log / `turns.jsonl`） |
| `docs/YIN_PERSONALIZATION_ENGINE.md` | **Yin Personalization Engine V1**（方向锁；L0/L1 本地运行时；≠ Memory / 品味层 / Qwen；L2 未拍板） |
| `docs/EMOTION_BIBLE.md` | 情绪/互动唯一权威清单；自主 vs 响应行为；文案标识符规范 |
| `docs/SCENE_ANIMATION_WIRING.md` | **场景→动画接线表**：用户时刻 × 档位 × 素材；v1.0.0 Slice A 范围 |
| `docs/HINTS_WIRING.md` | **场景→Hint 接线表**：时刻 × 互斥/门闩/宽窄批次；文案仍看 `ONBOARDING_HINTS` |
| `docs/CHARACTER_BIBLE.md` | 角色 Master Prompt 全文归档 + 项目关联澄清（Rive/莲花/蒲团/Suffix） |
| `docs/DESIGN.md` | 产品语义与玩法（专注、一炷香、沉睡唤醒等） |
| `docs/RESPONSIVE_LAYOUT.md` | **移动浏览器布局基线**：功能对等（竖/横屏）、竖屏 P1、可建议横屏；断点与验收矩阵 |
| `docs/PROCESS.md` | 协作流程、角色边界、**Backlog**、文首「当前进度速览」（重大进展后更新） |
| `docs/DEV_WORKFLOW_QUALITY.md` | **如何改善开发工作流来保证开发质量**：原则/规范/指引/注意事项；防假修好+防改坏叙事权威；门禁仍以 regression-lock 规则为准 |
| `docs/SHARED_RESOURCES.md` | 共享资源对照表（localStorage / EmotionController / Idle）；与 §2.3 高风险面互补 |
| `docs/RETENTION_FUNNEL.md` | **留存漏斗故事骨架**：首次打开 / 首次完成 / dayN 回访 / Honesty 桥接事件名与占位接线；无第三方 |
| `docs/COLLAB.md` | Task Brief / 文档更新协作约定 |
| `docs/TASKS.md` | 任务序列（按需查，勿全文常驻上下文） |
| `docs/TEST_TRACKER.md` | **功能验收追踪表**：用户可感知改动登记「待人工测试」；纯后端登记「仅单元测试覆盖」；用户书面意见进 **「用户反馈」列**（勿混入测试步骤）；缺陷须填 **严重度 + 处理承诺**（`RULES_INDEX` → `release-blocker-ledger`）；**关单级人工验收只认 `origin/develop` tip**（`qa-develop-tip`）；**标「已通过」须覆盖分工明示**（e2e 锁哪些 + 人工测哪些；`qa-pass-coverage-split`）；完成任务时须更新并在回复中说明需测项 |
| `docs/DEVELOP_DEBT_INVENTORY.md` | **develop 存量验证债务**：verified / assumed-ok / known-risky / unknown；排期用 |
| `docs/KNOWN_RISKY_TEST_CHECKLIST.md` | **known-risky 优先人工验收步骤**（Safari / 375 / 回流）；**仅本 MD 权威**。同目录 `known-risky-test-checklist.csv` 与根目录 `.numbers` **均不权威** |
| `docs/COVERAGE_GAP_AUDIT.md` | **功能 vs smoke/e2e 覆盖缺口审计**：模块对照矩阵、永不自动化清单、自动化后续顺序（Task 3→2→扩 smoke）；与 `TEST_TRACKER` §C / `SCENARIO_TESTS` 互补 |
| `docs/Z_INDEX.md` | **产品 z-index 层叠登记**（`RULES_INDEX` → `z-index-registry`）；新加 fixed / 改层级先查表 |
| `docs/INTERACTION_FEEDBACK_PRINCIPLES.md` | **点击反馈**：接收反馈 ≠ 结果反馈；禁止哑点击；PR 须答 0–1s（`RULES_INDEX` → `interaction-feedback`） |
| `docs/FEATURE_CONFLICT_REVIEW.md` | **实现前冲突扫描**：对照 `SCENARIO_TESTS` 的强度 / 语气 / 职责；有疑点须等拍板（`RULES_INDEX` → `feature-conflict-review`） |
| `docs/BACKGROUND_NETWORK.md` | **后台网络三问**：非用户点击的请求须先答时机 / 写盘 / 慢网动效（`RULES_INDEX` → `background-network`） |
| `docs/SILENT_BEHAVIORS.md` | **已知静默白名单**：不在表里的「没反应」测试时一律当 bug |
| `docs/LAB_SCRIPT_CONVENTIONS.md` | **L0 实验室脚本路径 / 调用 / 命名 / 陷阱 / 已测候选索引**（只指路，不存测试数据）；新会话同类任务先读本文 |
| `docs/RULES_INDEX.md` | **规则主题 → 唯一权威来源**（工作流 / Git / 回归锁 / 文档契约）；新增规则先查此表 |

## PRINCIPLES 硬性摘要

- **经济可持续（2026-08-11）**：发布已远不止「只验证 MVP」；短长期收益须平衡；禁止学雷锋式堆功能导致无付款动力；假收费须优先修。允许委婉付费感知；禁止 FOMO / 共鸣句硬推销。**案例锚定**：Reflection 底 Wisdom+静默印花＝允许；共鸣句硬推销＝禁止（见 `PRINCIPLES.md`）。静默彩蛋≠获客转化。
- **正念伙伴，不是传统电子宠物**：保留伙伴式互动；禁止喂养、健康退化、照料责任与宠物收集叙事。
- **诚实机制**：允许无验证补登别处完成的练习；与正常计时一视同仁；禁止怀疑性文案；不占系统提醒限频池。
- **观照者而非情绪本身**：一次性/响应性情绪必须「触发 → 表现 → 自动回归**类似坐禅**的坐姿呼吸基底」（不强制像素对齐默认闭目第 1 帧）；文案只作观察式描述，不贴标签、不追因、不说教。
- **序列衔接（CapCut 式叠代）**：两序列无法自然衔接时，定格末/首帧做约 **1s** 双层交叉淡化（`CAPCUT_DISSOLVE_MS` + `freezeUntilCrossFadeEnds`），**禁止闪切**；同源微表情可用 ~180ms。见 `PRINCIPLES.md` / `ARCHITECTURE.md` / `EMOTION_BIBLE.md` §1.6。
- **闭目坐禅节奏**：Idle = 呼吸（约 2.5fps）×5 → 眨眼 → 往复；禁止其它 Idle 变体。`Sleeping` 约 1 fps。见 `PRINCIPLES.md` / `EMOTION_BIBLE.md`。
- **宁静型游戏化**：regular practice, at your own pace；禁止强迫签到、概率奖励与街机式狂欢。
- **反馈分级**：每次完成轻量确认；每日首次达标完整庆祝；长期里程碑纪念奖励。
- **不制造焦虑**：默认态中性/温馨；禁止退步感视觉（灰败、悲伤、评判「不专注」因果）。  
- **永不消失**：只有打瞌睡等候，无死亡/离家出走苦情。  
- **一次只做一个任务**；禁止未立项超范围与占位空目录。  
- **路径必须使用英文 ASCII**：所有新增并由代码、构建流程或 Git 处理的文件/目录均用小写 `kebab-case`；禁止中文、空格、下划线、括号、省略号等特殊字符。用户提供的素材或压缩包须在入库前先按语义重命名（如「点头致意」→ `nod-greeting`），只豁免文件正文内容，不豁免文件名。完整规则见 `PRINCIPLES.md`「原则三」。
- 老虎与陪伴体验是主角，数据是配角；正向反馈要即时有情感。

## ARCHITECTURE 硬性摘要

- **主线情绪** = 2D PNG 序列（视频抽帧）；上层只调 `EmotionController.playEmotion()`。  
- **3D GLB + PoseManager/DynamicMotion**：不删，留给 Backlog「纪念奖励系统（金牌/环境细节 + 3D 公仔）」展示，不当主界面情绪载体。
- **绕 Y 轴旋转**：2D 主线不做同等替代；奖励柜可复用。  
- 文案：`src/locales/*.json` + `t()` / `tPool()`，禁止 UI 句子硬编码进业务逻辑。  
- 眼睛跟随：~~`EyeTracking`~~ **已废弃**；「偶尔看看」= Idle 呼吸×5→眨眼。张望素材默认不调度。

## EMOTION / 行为优先级摘要

- `Celebrating` ≫ `MindfulAcknowledge` / `IncenseComplete` ≫ 日常 `Idle`/`Smiling`。  
- **响应行为 > 自主行为**；自主层保持「不打扰」（少挥手、少长盯）。  
- Celebrating 期间摸头忽略、不排队。

## 开发时勿踩

- 不要在情绪主线任务里实现纪念奖励/成就墙 / 3D 公仔柜 / 数字木鱼等 Backlog。
- 不要把负面「惩罚用户」叙事写回睡眠/中断反馈。  
- 改情绪表现先改 `EMOTION_BIBLE.md`（或本任务已书面确认），再写代码。
- 预览浏览器与能耗 / 进程收尾 / Cloud 独立会话提醒 → `focus-tiger-browser-energy.mdc`（`RULES_INDEX` → `browser-energy`）；勿在本文件复述条款。
- Agent Token Cost（禁子 Agent / 禁轮询长 CI / 禁擅自全量 e2e）→ `focus-tiger-agent-token-cost.mdc`（`RULES_INDEX` → `agent-token-cost`）；勿在本文件复述条款。
- 列多个可执行方案时须同时给出「我认为最合理的」→ `focus-tiger-recommend-most-reasonable.mdc`（`RULES_INDEX` → `recommend-most-reasonable`）；勿在本文件复述条款。
- 会话交接 / 口令「生成交接」→ `focus-tiger-session-handoff.mdc`（`RULES_INDEX` → `session-handoff`）；勿在本文件复述模板。
- 调试/优化桌面本地 AI companion → `focus-tiger-companion-debug.mdc`（`RULES_INDEX` → `companion-debug`）；勿在本文件复述条款。实验室脚本路径/命名/已测候选 → `docs/LAB_SCRIPT_CONVENTIONS.md`，勿再全仓搜索。
- 可点击交互：PR 须答「0–1 秒内用户看到什么」；设计静默须挂 `SILENT_BEHAVIORS.md` 的 `SB-xx`。全文见 `INTERACTION_FEEDBACK_PRINCIPLES.md`（`RULES_INDEX` → `interaction-feedback`）；勿在本文件复述六条。
- 实现前冲突扫描 → `FEATURE_CONFLICT_REVIEW.md`（`RULES_INDEX` → `feature-conflict-review`）；勿在本文件复述三轴。
- 非用户点击的网络请求 → `BACKGROUND_NETWORK.md`（`RULES_INDEX` → `background-network`）；勿在本文件复述三问。

## TEST_TRACKER 日常维护（Part 2）

每次完成一项任务后：

1. **UI / 动画 / 文案 / 交互**可见改动 → 在 `docs/tracker-entries/<分支名>.md` 新增一行（`/` → `-`），状态「待人工测试」，写清测试步骤与本地访问路径，日期填当天。**不要**在 `TEST_TRACKER.md` 主表插行（见文首「新增行走碎片」）。
2. **纯后端 / 逻辑** → 同样写入碎片，登记「仅单元测试覆盖」，仍要登记。
3. 完成消息须写明「本次有 N 项需要你测试，已写入 tracker-entries/」；禁止仅以「已完成 / 已验收」收尾。正式邀测前须跑 `npm run check:branch-freshness` 并汇报落后数（`RULES_INDEX` → `branch-freshness`）；落后 > 0 不得代表 develop 邀测。  
   **拼装触发**（SSOT：`TEST_TRACKER.md`「拼装触发」）：功能 PR **禁止** `tracker:assemble`。口令「批量人工测试」前若有碎片 → 先独立 `docs/*` PR 拼装再出清单；待拼碎片 ≥ 5 → 下班前 Git 同步或下一次文档会话必须另开拼装 PR。
4. 单元测试通过 ≠ 用户验收；视觉项在用户确认前保持「待人工测试」。
5. 用户反馈问题 → 该行改「有问题」，**只写入「用户反馈」列**（日期 + 原话要点）；**禁止**混入「测试步骤」；同时填「严重度」「处理承诺」（见 `TEST_TRACKER`「缺陷分级与处理承诺」）；修复后再改回「待人工测试」等复测，**禁止**自行标「已通过」。
6. **用户书面测试意见（2026-07-19 起）**：即使本回合未立刻开修，也必须记入对应行的「用户反馈」列；见 `TEST_TRACKER.md` 文首「用户测试反馈记入规则」。书面意见若测自非 `origin/develop` tip → 仍记入反馈，但**不得**据此关单（见文首「人工验收唯一基线」）。
7. **标「已通过」**（关单）→ 必须满足 `TEST_TRACKER`「标「已通过」门禁」（`RULES_INDEX` → `qa-pass-coverage-split`）：tip 合法书面确认 + **同次回合写清 e2e/自动化已锁哪些场景与人工已覆盖哪些场景**；**禁止**仅凭 e2e 绿或笼统「测试 OK」关单。

## 回归锁（方法级 · 与上并列强制）

详见按需规则 `focus-tiger-regression-lock.mdc`、叙事全文 `docs/DEV_WORKFLOW_QUALITY.md`、摘要 `docs/PROCESS.md`「回归锁工作法」、主题权威索引 `docs/RULES_INDEX.md`。  
**勿在本文件复述** commit / 合并 main / §7 等完整条款；只保留指向：

- 交互修复完工门禁、Commit 汇报与分支门禁、Bug close §7 → `focus-tiger-regression-lock.mdc`
- 分支模型 / 合并 main / 跨会话冲突 / 并行 worktree / **姊妹分支同步** / **固定 QA develop 树** → 仓库根 `WORKFLOW.md`
- 质量叙事（why/how）→ `DEV_WORKFLOW_QUALITY.md`
- 如涉及运行测试/CI相关任务，参考 testing-strategy 规则。

## Git 同步（任务收尾默认 push + PR；禁止直推主干）

操作节奏摘要见 `PROCESS.md`「Git 同步节奏」（含口令「请安排下班前的 Git 同步」须先增量核对 `SCENARIO_TESTS.md` · `scenario-tests-eod-sync`；与「批量人工测试」）。政策 SSOT 见 `RULES_INDEX.md`（`git-agent-commit` / `git-merge-main` / `git-develop-small-pr-run-merge` / `prod-worker-deploy` / `scenario-tests-eod-sync`）与 regression-lock 第 5–7 条（**禁止直推 develop/main**；同步=旁支 + PR）。
`stop` 的 Git 同步 macOS 通知钩子已关闭（`hooks.json` 的 `stop` 为空）。若日后重新启用，只允许本地系统通知并返回 `{}`；禁止使用 `followup_message` 自动开启额外 Agent 回合。

禁止：post-commit 钩子自动 push、保存即 commit、未经确认把大体量抽帧素材 / `node_modules` 推上远程、静默 commit 不汇报、**直推 `develop`/`main`**、未说「部署」就 Redeploy 生产 Worker。详见 regression-lock 与 `WORKFLOW.md`。 
**注意**：只改代码、文档不进 commit = 假完成（下一会话权威口径仍是旧的）。 
**合入 `develop`**：CI 绿即可（见 `WORKFLOW.md` / `git-develop-small-pr-run-merge`）；**不等**人工测试。合 `main` 仍须你明确下令。TEST_TRACKER 保持「待人工测试」直到你关单。
