---
description: Focus Tiger 权威文档索引与硬性约束兜底（任务开始前必读摘要）
alwaysApply: true
---

# 坐禅小老虎 / Focus Tiger · 项目规则

代码与产品主目录：`focus-tiger/`。权威全文在 `focus-tiger/docs/`，**本规则仅摘要**；细则冲突时以对应文档正文为准。  
**不要**把持续增长的 `TASKS.md` 全文塞进规则；排期只在接任务时按需查阅。

## 任务开始前

1. 确认本次改动是否触碰下方红线 / 架构边界。  
2. 产品定位、范围、品牌措辞相关任务 → 先对照 `PRODUCT_POSITIONING.md`；用户、JTBD、指标、付费或隐私相关任务 → 先对照 `MVP_PRODUCT_DEFINITION.md`；情绪、互动、文案相关任务 → 先对照 `EMOTION_BIBLE.md`。
3. 模块归属不清 → 对照 `ARCHITECTURE.md` + `PROCESS.md` 角色分工。  
4. Prompt 未手写引用文档时，仍以本规则 + 文档路径为准，必要时主动打开对应 md。

## 权威文档地图

| 文档 | 用途 |
|---|---|
| `docs/PRODUCT_POSITIONING.md` | **品牌与产品战略顶层**：核心使命、正念伙伴定位、宁静型游戏化、反馈分级与长期边界；产品语义层级高于 `DESIGN.md` |
| `docs/MVP_PRODUCT_DEFINITION.md` | **MVP 产品策略基线**：首要用户、JTBD、竞争替代品、验证指标、付费假设与隐私承诺；假设不等同于已上线功能 |
| `docs/PRODUCT_MOMENTS.md` | **一天叙事框架（Five Moments）**：Arrive / Focus / Recover / Transition / Reflect；位于定位之下、`TASKS` 之上，不推翻已有实现 |
| `docs/CORE_LOOP.md` | **单次会话七步状态机**：Arrive→Focus→Recover→Reflect→Grow；与 Five Moments 的粒度关系 |
| `docs/ARRIVE_MOMENT_DESIGN.md` | Arrival Practice 交互详规（v2：Notice/Choose 点选为主） |
| `docs/LIGHT_PROGRESSION_DESIGN.md` | 光影物理渐进（2D DOM/CSS）：Arrival 氛围 + Recover 扰动；排除主界面 3D/Shader |
| `docs/PRINCIPLES.md` | **硬性红线**：不制造焦虑、永不消失、诚实机制、一次一任务、不超范围、性能与风格 |
| `docs/ARCHITECTURE.md` | 模块边界、主线 **2D PNG 序列**、3D 资产保留给奖励场景、`EmotionController` 桥梁 |
| `docs/EMOTION_BIBLE.md` | 情绪/互动唯一权威清单；自主 vs 响应行为；文案标识符规范 |
| `docs/CHARACTER_BIBLE.md` | 角色 Master Prompt 全文归档 + 项目关联澄清（Rive/莲花/蒲团/Suffix） |
| `docs/DESIGN.md` | 产品语义与玩法（专注、一炷香、沉睡唤醒等） |
| `docs/RESPONSIVE_LAYOUT.md` | **移动浏览器布局基线**：功能对等（竖/横屏）、竖屏 P1、可建议横屏；断点与验收矩阵 |
| `docs/PROCESS.md` | 协作流程、角色边界、**Backlog**、文首「当前进度速览」（重大进展后更新） |
| `docs/DEV_WORKFLOW_QUALITY.md` | **如何改善开发工作流来保证开发质量**：原则/规范/指引/注意事项；防假修好+防改坏叙事权威；门禁仍以 regression-lock 规则为准 |
| `docs/SHARED_RESOURCES.md` | 共享资源对照表（localStorage / EmotionController / Idle）；与 §2.3 高风险面互补 |
| `docs/RETENTION_FUNNEL.md` | **留存漏斗故事骨架**：首次打开 / 首次完成 / dayN 回访 / Honesty 桥接事件名与占位接线；无第三方 |
| `docs/COLLAB.md` | Task Brief / 文档更新协作约定 |
| `docs/TASKS.md` | 任务序列（按需查，勿全文常驻上下文） |
| `docs/TEST_TRACKER.md` | **功能验收追踪表**：用户可感知改动登记「待人工测试」；纯后端登记「仅单元测试覆盖」；用户书面意见进 **「用户反馈」列**（勿混入测试步骤）；完成任务时须更新并在回复中说明需测项 |

## PRINCIPLES 硬性摘要

- **正念伙伴，不是传统电子宠物**：保留伙伴式互动；禁止喂养、健康退化、照料责任与宠物收集叙事。
- **诚实机制**：允许无验证补登别处完成的练习；与正常计时一视同仁；禁止怀疑性文案；不占系统提醒限频池。
- **观照者而非情绪本身**：一次性/响应性情绪必须「触发 → 表现 → 自动回归**类似坐禅**的坐姿呼吸基底」（不强制像素对齐默认闭目第 1 帧）；文案只作观察式描述，不贴标签、不追因、不说教。
- **序列衔接（CapCut 式叠代）**：两序列无法自然衔接时，定格末/首帧做约 **1s** 双层交叉淡化（`CAPCUT_DISSOLVE_MS` + `freezeUntilCrossFadeEnds`），**禁止闪切**；同源微表情可用 ~180ms。见 `PRINCIPLES.md` / `ARCHITECTURE.md` / `EMOTION_BIBLE.md` §1.6。
- **闭目坐禅节奏**：Idle = 呼吸（约 2.5fps）×5 → 眨眼 → 往复；禁止其它 Idle 变体。`Sleeping` 约 1 fps。见 `PRINCIPLES.md` / `EMOTION_BIBLE.md`。
- **宁静型游戏化**：regular practice, at your own pace；禁止强迫签到、概率奖励与街机式狂欢。
- **反馈分级**：每次完成轻量确认；每日首次达标完整庆祝；长期里程碑纪念奖励。
- **不制造焦虑**：默认态中性/温馨；禁止退步感视觉（灰败、悲伤、评判「不专注」因果）。  
- **永不消失**：只有打瞌睡等候，无死亡/离家出走苦情。  
- **一次只做一个任务**；禁止未立项超范围与占位空目录。  
- **路径必须使用英文 ASCII**：所有新增并由代码、构建流程或 Git 处理的文件/目录均用小写 `kebab-case`；禁止中文、空格、下划线、括号、省略号等特殊字符。用户提供的素材或压缩包须在入库前先按语义重命名（如「点头致意」→ `nod-greeting`），只豁免文件正文内容，不豁免文件名。完整规则见 `PRINCIPLES.md`「原则三」。
- 老虎与陪伴体验是主角，数据是配角；正向反馈要即时有情感。

## ARCHITECTURE 硬性摘要

- **主线情绪** = 2D PNG 序列（视频抽帧）；上层只调 `EmotionController.playEmotion()`。  
- **3D GLB + PoseManager/DynamicMotion**：不删，留给 Backlog「纪念奖励系统（金牌/环境细节 + 3D 公仔）」展示，不当主界面情绪载体。
- **绕 Y 轴旋转**：2D 主线不做同等替代；奖励柜可复用。  
- 文案：`src/locales/*.json` + `t()` / `tPool()`，禁止 UI 句子硬编码进业务逻辑。  
- 眼睛跟随：~~`EyeTracking`~~ **已废弃**；「偶尔看看」= Idle 呼吸×5→眨眼。张望素材默认不调度。

## EMOTION / 行为优先级摘要

- `Celebrating` ≫ `MindfulAcknowledge` / `IncenseComplete` ≫ 日常 `Idle`/`Smiling`。  
- **响应行为 > 自主行为**；自主层保持「不打扰」（少挥手、少长盯）。  
- Celebrating 期间摸头忽略、不排队。

## 开发时勿踩

- 不要在情绪主线任务里实现纪念奖励/成就墙 / 3D 公仔柜 / 数字木鱼等 Backlog。
- 不要把负面「惩罚用户」叙事写回睡眠/中断反馈。  
- 改情绪表现先改 `EMOTION_BIBLE.md`（或本任务已书面确认），再写代码。

## TEST_TRACKER 日常维护（Part 2）

每次完成一项任务后：

1. **UI / 动画 / 文案 / 交互**可见改动 → 在 `docs/TEST_TRACKER.md` 新增一行，状态「待人工测试」，写清测试步骤与本地访问路径，日期填当天。
2. **纯后端 / 逻辑** → 登记「仅单元测试覆盖」，仍要登记。
3. 完成消息须写明「本次有 N 项需要你测试，已更新 TEST_TRACKER.md」；禁止仅以「已完成 / 已验收」收尾。
4. 单元测试通过 ≠ 用户验收；视觉项在用户确认前保持「待人工测试」。
5. 用户反馈问题 → 该行改「有问题」，**只写入「用户反馈」列**（日期 + 原话要点）；**禁止**混入「测试步骤」；修复后再改回「待人工测试」等复测，**禁止**自行标「已通过」。
6. **用户书面测试意见（2026-07-19 起）**：即使本回合未立刻开修，也必须记入对应行的「用户反馈」列；见 `TEST_TRACKER.md` 文首「用户测试反馈记入规则」。

## 回归锁（方法级 · 与上并列强制）

详见 alwaysApply 规则 `focus-tiger-regression-lock.mdc`、叙事全文 `docs/DEV_WORKFLOW_QUALITY.md`、摘要 `docs/PROCESS.md`「回归锁工作法」。摘要：

- 交互修复 = 主路径 + **回流路径**；禁止只验一次 Happy Path 就声称修好。  
- 门闩未就绪 → UI 禁用，禁止可点却静默 `return`。  
- 门闩行为须有失败用例单测；**确认修复的 bug 须留回归锚**（不限门闩；或 TEST_TRACKER 标明人工锁路径）。  
- 声称修好前先跑 **`npm run test:smoke` 与 `npm run test:e2e`**（场景 A–D 逻辑 + 产品壳 DOM；全绿 ≠ 序列观感通过）。  
- 同主题 TEST_TRACKER 行禁止互斥步骤；**观感子项须分列**，禁止笼统一行勾选假完成。  
- **防把好的改坏**：改前列已好清单；重写须继承观感契约；序列衔接（Idle 眨眼、cross-fade、Choose/Rise 叠化）进高风险表。  
- **所有实质性 Task 收尾后可自动本地 commit**（交互修复 / 文档 / 调研 / 素材 / 回归锁均适用）：提交到**当前工作分支**（`develop` / `feature/*` / `fix/*`）；**commit 后同回合须汇报** hash + 分支 + 涉及文件（禁止静默提交；并行会话同规）；**禁止**任何会话自动合并进 `main`；**仍禁止**未确认 push。详见 regression-lock「Commit 汇报与分支门禁」。 
- **跨会话指令冲突**：开 PR / 合并 `main` / push 前（距上次同类操作约 10–15 分钟以上）须先查仓库客观状态；发现更晚 PR/tip/CI 活动须先问用户。见 `WORKFLOW.md` 与 regression-lock「跨会话指令冲突处理」。 
- **已完成且验证通过的改动不得跨任务周期悬置未 commit**：代码与纯文档一视同仁；验证方式按任务类型取 `docs:check` / 相关测试 / 对应自检。  
- **每个 commit 对应一个逻辑完整改动**（功能 / bug 修复 / 文档更新），而不是按时间片零碎提交。  
- **commit message 必须写明 what + why**；禁止 `update docs` / `misc fix` / `wip` 之类无信息量提交。  
- **Bug 修复一体包（N15）**：代码或修正措施 + **相关项目文档同批更新**（至少 `TEST_TRACKER`；按触及面扩权威 md）+ **立刻本地 commit** + **同回合汇报**；缺一 = 未完成，不得写「已修好」。  
- **Bug close（§7）**：声称「已修复」须 **红绿对照 + CI 绿 + push + 文档口径 + 人工体验确认**；回复必列 checklist；本地 commit/本地冒烟 **不等于** 修复完成（见 `DEV_WORKFLOW_QUALITY.md` §7、`PROCESS.md` §D）。  
- 每次任务汇报末尾须有独立 **「待你决定 / 待你知道」** 清单（N14；禁止只在正文带过）。

## Git 同步（半自动，禁止静默 push）

实质性 Task / Bug 修复结束后（与 `PROCESS.md`「当前进度速览」维护规则对齐）：

1. 更新速览  
2. **同步相关权威文档**（N15；至少 `TEST_TRACKER`）  
3. **所有实质性 Task**：收尾后可 **自动** `git commit` 到当前工作分支（**代码 + 文档**；message 含关键词）；**同回合汇报** hash + 分支 + 文件；**禁止静默提交**；并行会话同规  
4. 其它大范围/素材/不确定范围改动：先说明将提交路径再 commit  
5. 可选运行 `./scripts/git-sync-safe.sh` 做推送前体检  
6. **仅当用户明确要求 push 时**再执行 `./scripts/git-sync-safe.sh --push` 或 `git push`  
7. **合并进 `main`**（含 `git merge develop` → `main` 等）**永远**须用户明确指令；任何会话不得自动执行  

`stop` 的 Git 同步 macOS 通知钩子已关闭（`hooks.json` 的 `stop` 为空）。若日后重新启用，只允许本地系统通知并返回 `{}`；禁止使用 `followup_message` 自动开启额外 Agent 回合。

禁止：post-commit 自动 push、保存即 commit、未经确认把大体量抽帧素材 / `node_modules` 推上远程、任何会话自动合并进 `main`、静默 commit 不汇报。  
**注意**：本地 commit 固化基线；真正需人工闸门的是 **push** 与 **合并进 `main`**。  
**注意**：只改代码、文档不进 commit = 假完成（下一会话权威口径仍是旧的）。
