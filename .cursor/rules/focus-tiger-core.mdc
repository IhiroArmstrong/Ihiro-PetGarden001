---
description: Focus Tiger 核心常驻层——Git 硬闸、N14 收尾、测试输出卫生、按需加载索引（其余规则触发时再 Read）
alwaysApply: true
---

# Focus Tiger · 核心层（常驻）

产品目录：`focus-tiger/`。规则主题索引：`focus-tiger/docs/RULES_INDEX.md`。  
**本文件以外**的 `.cursor/rules/focus-tiger-*.mdc` 与 `testing-strategy.mdc` 均为**按需加载**——命中下方索引时须 `Read` 全文，不得凭记忆省略条款。

## Git 硬闸

> 分支/PR/合入细则 SSOT：`WORKFLOW.md`（`RULES_INDEX` → `git-agent-commit` / `git-pr-base-develop`）。

- **禁止** `git push` 到 `origin/develop` / `origin/main`；进主干只能 PR 合入。
- 任务验证通过后：**默认** `git commit`（旁支 `feature/*` / `fix/*` / `docs/*`）→ **默认** `git push -u origin HEAD` → 开/更新 `--base develop` 的 PR；同回合汇报 hash、分支、文件、PR URL。
- **禁止**自动合并进 `main`；合 `develop` 须 CI 绿（细则按需读 `focus-tiger-regression-lock.mdc`）。
- 代码与文档同批 commit；只改代码、文档不进 commit = 假完成。
- 禁止静默 commit（commit 后须同回合汇报 hash、分支、文件）。
- **禁止** post-commit **钩子**静默 push、保存即 commit、未经确认把大体量抽帧素材 / `node_modules` 推上远程（与「任务收尾默认 push 旁支 + 开 PR」不矛盾；细则见 `focus-tiger-regression-lock.mdc`「Commit 汇报与分支门禁」）。

## 任务纪律

- **一次一任务**；新文件/目录路径须英文 ASCII、`kebab-case`（全文见 `PRINCIPLES.md`）。
- 接**实现类**任务前 → 先 `Read` **`focus-tiger-docs.mdc`**。
- 声称修好 / 交互修复收尾 / commit·push 前 → 先 `Read` **`focus-tiger-regression-lock.mdc`**。

## 收尾（N14 · 每次用户可见回复末尾）

独立小节 **「待你决定 / 待你知道」**（无事项也须标题 +「无」）。  
列 ≥2 个可执行开放方案时须写 **我认为最合理的**（细则按需读 `focus-tiger-recommend-most-reasonable.mdc`）。

## 测试输出卫生（强制）

测试输出卫生规则见 `testing-strategy.mdc` §「读取 CI / 本地测试结果时的规则」，此处不重复。

## 按需加载索引

| 触发场景 | 先 Read |
|---|---|
| 接任务 / 改产品语义 / 情绪·架构·文案 | `focus-tiger-docs.mdc` |
| bug 修复·交互修复·完工·commit·push·§7 | `focus-tiger-regression-lock.mdc` |
| 起 Vite·预览·Safari 验收·IDE Browser | `focus-tiger-browser-energy.mdc` |
| 跑测试·e2e·CI·控 token | `testing-strategy.mdc` + `focus-tiger-agent-token-cost.mdc` |
| 关单 QA·5173·PR 合 develop 后 | `focus-tiger-qa-develop-worktree.mdc`（`WORKFLOW.md` · `qa-develop-worktree`） |
| 口令「生成交接」·阶段性收尾 | `focus-tiger-session-handoff.mdc` |
| 记录未闭环 bug/根因扇出·「核对问题清单」 | `focus-tiger-issue-ledger.mdc` |
| 实现前冲突扫描 | `focus-tiger-feature-conflict-review.mdc` |
| 可点击交互·PR 反馈三问 | `focus-tiger-interaction-feedback.mdc` |
| 非用户点击的网络请求 | `focus-tiger-background-network.mdc` |
| 调试本地 AI companion | `focus-tiger-companion-debug.mdc` |

## 产品红线

产品红线与架构硬性摘要见 `focus-tiger-docs.mdc` §「PRINCIPLES 硬性摘要」「ARCHITECTURE 硬性摘要」，此处不重复。
