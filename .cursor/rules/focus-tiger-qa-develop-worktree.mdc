---
description: 固定 develop QA worktree——关单验收/批量人工测试/PR 合 develop 后 sync:qa-develop 前须 Read
alwaysApply: false
---

# Focus Tiger · 固定 develop 验收 worktree（按需层）

> **加载时机**：关单级人工验收、口令「批量人工测试」、PR 已合入 develop、或涉及 5173 QA 树时。

索引：`RULES_INDEX.md` → `qa-develop-worktree`。  
**SSOT**：仓库根 `WORKFLOW.md`「固定 develop 验收 worktree」。本文件只留强制动作，**不**复述建树命令全文。

## 强制（本会话）

1. **关单验收 / 批量人工测试** 只用固定树 `…-wt-develop-qa`、端口 **5173**（`npm run dev:qa`）。**不用于开发**。feature/fix 仍走各自 `…-wt-<topic>`，开发流程不变。批量清单见 `TEST_TRACKER.md`。  
2. **任何 PR 已合并进 `develop` 之后**（含本回合 merge / auto-merge 完成 / 发现 `origin/develop` 已前进）：必须跑 `cd focus-tiger && npm run sync:qa-develop`。本机有该树时脚本会 `git pull --ff-only origin develop`。  
3. **向用户明确写出**：  
   - **①** 是否需要重启 QA Vite（脚本 `restart:`：依赖或构建配置改动才重启，否则硬刷新即可）  
   - **②** 一句这次合并带来什么变化（脚本 `summary:`）  
4. **Cloud / 本机无此树**：不得假装已在 Mac 上 pull；汇报须含 `qa_worktree: ABSENT`，①② 仍要写。  
5. **禁止**为收尾停掉 QA 树 5173 Vite；禁止在该树 `git commit`。  
6. **5173 正在测时**：禁止抢端口、禁止 `git switch` 正在出码的目录。只允许另建 detached QA 树；等这轮测完再 `npm run dev:qa`。  
7. **Cloud Apply / checkout migrated branch** 视同抢检出：禁止在主仓、本 QA 树、或正在出 5173 的目录上点。落到本机须另开 `…-wt-…`（`WORKFLOW.md` 并行 worktree 第 8 款）。

## 复测提醒（强制 · 邀测 / 声称已修 / 改动看似无效）

凡本回合**改了代码**并请你复测、或你反馈「改了但没效果」时，Agent **禁止**只写「再试一遍 / 硬刷新 / 清缓存」。

**必须同时写明：**

1. **测哪棵树**：5173 固定 QA 树（`…-wt-develop-qa`）还是 feature 主仓旁支（另开端口，如 5174）。二者不可混用。  
2. **QA 树是否已 sync**：若测 5173 → 须先 `npm run sync:qa-develop`（或确认本回合已跑过）；写明 ① 是否须重启 QA Vite ② 一句变化。  
3. **代码指纹（可选但推荐）**：DevTools 跑 `fetch('/src/main.js').then(r=>r.text()).then(t=>console.log(t.includes('<本次关键符号>')))`，须为 `true` 才继续功能验收。

**「改动无效」高概率根因**：浏览器仍在旧树（常见：主仓改了代码但 5173 仍跑 QA 树旧 tip）。此时先查「谁在监听该端口 + 服务端 main.js 是否含本次符号」，再谈下一版补丁。
